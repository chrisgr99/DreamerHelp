'plugin': 'RSBATechModules'
'source': 'https://github.com/rjsmith/rsbatech-modules/wiki'
'read': '2026-09-14'
'modules':
  'OrestesOne':
    'lines':
      - 'Maps the parameters of other modules in the patch onto an Electra One MIDI controller, sending each mapped parameter''s name and value to the controller''s screen and taking 14-bit changes back from it.'
      - 'Steps back through the modules in the patch that have a mapping in the library, in rack order, and loads the mapping of the one it reaches.'
      - 'Steps forward through the modules in the patch that have a mapping in the library, in rack order, and loads the mapping of the one it reaches.'
      - 'Arms a click: the pointer becomes a crosshair and the next module clicked has its library mapping loaded into the slots. A lamp is lit while it is armed.'
      - 'Note — nothing reaches the controller until a MIDI device is chosen on the port selectors and the matching Electra One preset is running, at the same major and minor version as the plugin.'
      - 'Note — the four port selectors choose the input and output device for the Electra One''s data port and for its control port. They are not parameters, so clicking one opens its device list.'
      - 'Note — the list filling the middle of the panel is the mapping table, one row per controller slot. Each row can be dragged onto a parameter to map it, and each row has a right-click menu of its own.'
      - 'Note — mappings are kept in a library file outside the patch, so a mapping built once is available to every patch. The patch itself stores the slots currently loaded.'
      - 'Note — the plugin ships a factory library of module mappings which is read in the first time the module finds no library file.'
      - 'Menu — Preset load — opens the two settings for what a loaded preset is allowed to overwrite.'
      - 'Menu — Ignore MIDI devices — keeps the device chosen on the panel when a preset is loaded, rather than taking the one the preset names.'
      - 'Menu — Clear mapping slots — empties every mapping slot when a preset is loaded.'
      - 'Menu — Precision: High, Medium, Low — sets how often parameter values are read and sent, as a division of the sample rate; the menu prints the resulting rate in Hz.'
      - 'Menu — Mode: Operating, Locate and indicate — Operating lets an incoming value move the mapped parameter; Locate and indicate leaves the value alone and flags the mapped parameter on screen instead.'
      - 'Menu — Re-send MIDI feedback — opens the two ways of pushing current values back to the controller.'
      - 'Menu — Now — sends every mapped parameter''s name and value to the controller once.'
      - 'Menu — Periodically — keeps re-sending every mapped parameter''s name and value at intervals.'
      - 'Menu — User interface — opens the three settings for what the mapping list shows and whether it can be edited.'
      - 'Menu — Text scrolling — scrolls a mapping row''s text when it is wider than the row.'
      - 'Menu — Hide mapping indicators — stops the coloured marks drawn over mapped parameters elsewhere in the rack.'
      - 'Menu — Lock mapping slots — refuses changes to the mapping list, so a slot cannot be cleared or re-learned by accident.'
      - 'Menu — Scroll to selected module — moves the rack view to centre a module when its mapping is applied.'
      - 'Menu — Status overlay — shows a message over the rack naming the module whose mapping was applied.'
      - 'Menu — Automap this rack and save — opens the two ways of building a mapping for every module in the patch at once and writing them to the library.'
      - 'Menu — Skip pre-mapped modules — builds mappings only for modules the library does not already cover.'
      - 'Menu — Overwrite pre-mapped modules — builds a fresh mapping for every module, replacing what the library holds.'
      - 'Menu — Map module (select) — opens the three ways of arming a click on one module in the rack.'
      - 'Menu — Automap and save — the next module clicked has a mapping built for it and written to the library.'
      - 'Menu — Clear first — empties the mapping slots, then maps the next module clicked.'
      - 'Menu — Keep MIDI assignments — maps the next module clicked onto the slots already in use, leaving the controller assignments alone.'
      - 'Menu — Add module to library — lists every module in the patch; picking one writes the current mapping slots into the library under that module''s name.'
      - 'Menu — Save rack-level mapping — stores the current slots as a mapping for the whole patch rather than for one module.'
      - 'Menu — Clear mappings — empties every mapping slot.'
      - 'Menu — Apply module mapping — arms a click: the next module clicked has its library mapping loaded into the slots.'
      - 'Menu — Apply rack-level mapping — loads the mapping stored for the whole patch back into the slots.'
      - 'Menu — Autosave changes into mapping library file — writes the library file every time a mapping changes.'
      - 'Menu — Manage library mappings — lists the library by maker and module, with Delete on a module and Delete or Export on a maker.'
      - 'Menu — Import module mappings from file — opens the two ways of reading mappings out of a file you choose.'
      - 'Menu — Import module mappings from Factory Library — opens the two ways of reading in the mappings that ship with the plugin.'
      - 'Menu — Skip pre-mapped modules... — reads in only the mappings for modules the library does not already cover.'
      - 'Menu — Overwrite pre-mapped modules... — reads in every mapping, replacing what the library holds.'
      - 'Menu — Save mapping library file as... — writes the library to a new file and works from that file afterwards.'
      - 'Menu — Change mapping library file... — picks an existing library file to work from.'
      - 'Menu — Create empty mapping library file... — starts a new, empty library file and works from that.'
      - 'Menu — Clear MIDI assignment — on a row of the mapping list: drops the controller assignment from that slot.'
      - 'Menu — Input mode for NPRN: Direct, Pickup (snap), Pickup (jump), Toggle, Toggle + Value — on a row of the mapping list: sets how that slot turns an incoming value into a parameter move.'
      - 'Menu — Presets: Default, Inverted, Lower 50%, Upper 50% — on a row of the mapping list: sets which part of the parameter''s range the controller sweeps, and in which direction.'
      - 'Menu — Custom label — on a row of the mapping list: replaces the name sent to the controller for that slot, up to 20 characters.'
      - 'Menu — ORESTES-ONE — a section added to the right-click menu of any knob, slider or switch in the rack, for mapping that control to a slot from the control''s own end.'
    'param':
      '0': 3
      '1': 1
      '2': 2
    'notes': 'The three buttons carry no lettering on the panel; their names here come from the configParam strings at OrestesOne.cpp:469-471 and from what expMemPrevModule, expMemNextModule and enableLearn(LEARN_MODE::MEM) do in OrestesOneWidget.cpp:303-318. The module has no ports of any kind. The theme item the plugin puts on every module''s menu is left out as cosmetic.'
  'Pylades':
    'lines':
      - 'Maps the parameters of other modules in the patch onto a TouchOSC layout over the network, sending each mapped parameter''s name and value out as OSC and taking 14-bit changes back.'
      - 'Switches the OSC receiver on and off, opening or closing the receive port. The lamp is grey while it is off, green while it is on, and blue for a moment as each message arrives.'
      - 'Switches the OSC sender on and off, opening or closing the connection to the address and send port. The lamp is grey while off, green while on, blue as a message goes out.'
      - 'Steps back through the modules in the patch that have a mapping in the library, in rack order, and loads the mapping of the one it reaches.'
      - 'Steps forward through the modules in the patch that have a mapping in the library, in rack order, and loads the mapping of the one it reaches.'
      - 'Arms a click: the pointer becomes a crosshair and the next module clicked has its library mapping loaded into the slots. A lamp is lit while it is armed.'
      - 'Note — nothing passes either way until the sender and the receiver are switched on; both lamps are grey when the module is placed.'
      - 'Note — the address and the two port numbers are typed into fields on the panel, not chosen from a list; they start at localhost, 8881 to receive on and 8880 to send to.'
      - 'Note — the matching TouchOSC layout has to be running on the device, and the layout and the plugin have to agree on their major and minor version numbers.'
      - 'Note — the list filling the middle of the panel is the mapping table, one row per controller slot. Each row can be dragged onto a parameter to map it, and each row has a right-click menu of its own.'
      - 'Note — mappings are kept in a library file outside the patch, so a mapping built once is available to every patch. The patch itself stores the slots currently loaded.'
      - 'Menu — Preset load — opens the two settings for what a loaded preset is allowed to overwrite.'
      - 'Menu — Ignore OSC devices — keeps the device chosen on the panel when a preset is loaded, rather than taking the one the preset names.'
      - 'Menu — Clear mapping slots — empties every mapping slot when a preset is loaded.'
      - 'Menu — Precision: High, Medium, Low — sets how often parameter values are read and sent, as a division of the sample rate; the menu prints the resulting rate in Hz.'
      - 'Menu — Mode: Operating, Locate and indicate — Operating lets an incoming value move the mapped parameter; Locate and indicate leaves the value alone and flags the mapped parameter on screen instead.'
      - 'Menu — Re-send OSC feedback — opens the two ways of pushing current values back to the controller.'
      - 'Menu — Now — sends every mapped parameter''s name and value to the controller once.'
      - 'Menu — Periodically — keeps re-sending every mapped parameter''s name and value at intervals.'
      - 'Menu — User interface — opens the three settings for what the mapping list shows and whether it can be edited.'
      - 'Menu — Text scrolling — scrolls a mapping row''s text when it is wider than the row.'
      - 'Menu — Hide mapping indicators — stops the coloured marks drawn over mapped parameters elsewhere in the rack.'
      - 'Menu — Lock mapping slots — refuses changes to the mapping list, so a slot cannot be cleared or re-learned by accident.'
      - 'Menu — Scroll to selected module — moves the rack view to centre a module when its mapping is applied.'
      - 'Menu — Status overlay — shows a message over the rack naming the module whose mapping was applied.'
      - 'Menu — Automap this rack and save — opens the two ways of building a mapping for every module in the patch at once and writing them to the library.'
      - 'Menu — Skip pre-mapped modules — builds mappings only for modules the library does not already cover.'
      - 'Menu — Overwrite pre-mapped modules — builds a fresh mapping for every module, replacing what the library holds.'
      - 'Menu — Map module (select) — opens the three ways of arming a click on one module in the rack.'
      - 'Menu — Automap and save — the next module clicked has a mapping built for it and written to the library.'
      - 'Menu — Clear first — empties the mapping slots, then maps the next module clicked.'
      - 'Menu — Keep OSC assignments — maps the next module clicked onto the slots already in use, leaving the controller assignments alone.'
      - 'Menu — Add module to library — lists every module in the patch; picking one writes the current mapping slots into the library under that module''s name.'
      - 'Menu — Save rack-level mapping — stores the current slots as a mapping for the whole patch rather than for one module.'
      - 'Menu — Clear mappings — empties every mapping slot.'
      - 'Menu — Apply module mapping — arms a click: the next module clicked has its library mapping loaded into the slots.'
      - 'Menu — Apply rack-level mapping — loads the mapping stored for the whole patch back into the slots.'
      - 'Menu — Autosave changes into mapping library file — writes the library file every time a mapping changes.'
      - 'Menu — Manage library mappings — lists the library by maker and module, with Delete on a module and Delete or Export on a maker.'
      - 'Menu — Import module mappings from file — opens the two ways of reading mappings out of a file you choose.'
      - 'Menu — Import module mappings from Factory Library — opens the two ways of reading in the mappings that ship with the plugin.'
      - 'Menu — Skip pre-mapped modules... — reads in only the mappings for modules the library does not already cover.'
      - 'Menu — Overwrite pre-mapped modules... — reads in every mapping, replacing what the library holds.'
      - 'Menu — Save mapping library file as... — writes the library to a new file and works from that file afterwards.'
      - 'Menu — Change mapping library file... — picks an existing library file to work from.'
      - 'Menu — Create empty mapping library file... — starts a new, empty library file and works from that.'
      - 'Menu — Clear OSC assignment — on a row of the mapping list: drops the controller assignment from that slot.'
      - 'Menu — Input mode for NPRN: Direct, Pickup (snap), Pickup (jump), Toggle, Toggle + Value — on a row of the mapping list: sets how that slot turns an incoming value into a parameter move.'
      - 'Menu — Presets: Default, Inverted, Lower 50%, Upper 50% — on a row of the mapping list: sets which part of the parameter''s range the controller sweeps, and in which direction.'
      - 'Menu — Custom label — on a row of the mapping list: replaces the name sent to the controller for that slot, up to 20 characters.'
      - 'Menu — PYLADES — a section added to the right-click menu of any knob, slider or switch in the rack, for mapping that control to a slot from the control''s own end.'
    'param':
      '0': 1
      '1': 2
      '2': 5
      '3': 3
      '4': 4
    'notes': 'Panel lettering reads OSC sender over the button the code wires to PARAM_SEND and OSC receiver over PARAM_RECV (PyladesWidget.cpp:318-326 against res/Pylades.svg); the two are the right way round. The three buttons along the bottom carry no lettering; their names come from the configParam strings at Pylades.cpp:433-437. The receiving and sending flags at Pylades.cpp:322-324 are declared without an initialiser and onReset does not set them, so the state the module wakes in is whatever the allocation left; in practice both read off. The module has no ports of any kind. The theme item the plugin puts on every module''s menu is left out as cosmetic.'
