'plugin': 'ASK'
'source': 'https://github.com/pawal/ask-vcv-modules/blob/main/README.md'
'read': '2026-09-14'
'modules':
  'rnsh':
    'lines':
      - 'Eight white noise sources, each giving a fresh random voltage on the rising edge of its own gate, or running as continuous noise while that gate is unpatched'
      - 'Scales every output together, silence fully left and full level fully right; it is ignored entirely while anything is patched to CV'
      - 'A rising edge above 2V draws one fresh random voltage for the Out jack on that row, which is then held until the next edge'
      - 'Scales every output together in place of the Spread knob, 0V for silence and 10V for full level; past 10V the outputs are scaled beyond full, and a negative voltage inverts them'
      - 'Gaussian white noise scaled by Spread; with the Gate jack on that row patched the voltage changes only on a gate edge, and with it unpatched the noise runs continuously'
      - 'Note — patching anything into CV cuts the Spread knob out altogether: the knob is read only while CV is empty.'
      - 'Note — each output draws its own noise value, so no two outputs ever carry the same voltage.'
      - 'Note — the gate threshold is 2V rising, and the gate has to fall below 0.1V before it can fire again.'
    'param':
      '0': 1
    'in':
      '0': 2
      '1': 2
      '2': 2
      '3': 2
      '4': 2
      '5': 2
      '6': 2
      '7': 2
      '8': 3
    'out':
      '0': 4
      '1': 4
      '2': 4
      '3': 4
      '4': 4
      '5': 4
      '6': 4
      '7': 4
    'family':
      'in':
        '0': 'trigger'
        '1': 'trigger'
        '2': 'trigger'
        '3': 'trigger'
        '4': 'trigger'
        '5': 'trigger'
        '6': 'trigger'
        '7': 'trigger'
        '8': 'cv'
    'props':
      'in':
        '0':
          'poly': false
          'range': 'high above 2V'
          'step': 'stepped'
          'negative': 'ignored'
          'normal': '0V'
          'why': 'src/rnsh.cpp processGate(): triggers[n].process(rescale(getVoltage(), 0.1f, 2.f, 0.f, 1.f)) — one voltage read per frame, no channel loop'
        '1':
          'poly': false
          'range': 'high above 2V'
          'step': 'stepped'
          'negative': 'ignored'
          'normal': '0V'
          'why': 'src/rnsh.cpp processGate(): triggers[n].process(rescale(getVoltage(), 0.1f, 2.f, 0.f, 1.f)) — one voltage read per frame, no channel loop'
        '2':
          'poly': false
          'range': 'high above 2V'
          'step': 'stepped'
          'negative': 'ignored'
          'normal': '0V'
          'why': 'src/rnsh.cpp processGate(): triggers[n].process(rescale(getVoltage(), 0.1f, 2.f, 0.f, 1.f)) — one voltage read per frame, no channel loop'
        '3':
          'poly': false
          'range': 'high above 2V'
          'step': 'stepped'
          'negative': 'ignored'
          'normal': '0V'
          'why': 'src/rnsh.cpp processGate(): triggers[n].process(rescale(getVoltage(), 0.1f, 2.f, 0.f, 1.f)) — one voltage read per frame, no channel loop'
        '4':
          'poly': false
          'range': 'high above 2V'
          'step': 'stepped'
          'negative': 'ignored'
          'normal': '0V'
          'why': 'src/rnsh.cpp processGate(): triggers[n].process(rescale(getVoltage(), 0.1f, 2.f, 0.f, 1.f)) — one voltage read per frame, no channel loop'
        '5':
          'poly': false
          'range': 'high above 2V'
          'step': 'stepped'
          'negative': 'ignored'
          'normal': '0V'
          'why': 'src/rnsh.cpp processGate(): triggers[n].process(rescale(getVoltage(), 0.1f, 2.f, 0.f, 1.f)) — one voltage read per frame, no channel loop'
        '6':
          'poly': false
          'range': 'high above 2V'
          'step': 'stepped'
          'negative': 'ignored'
          'normal': '0V'
          'why': 'src/rnsh.cpp processGate(): triggers[n].process(rescale(getVoltage(), 0.1f, 2.f, 0.f, 1.f)) — one voltage read per frame, no channel loop'
        '7':
          'poly': false
          'range': 'high above 2V'
          'step': 'stepped'
          'negative': 'ignored'
          'normal': '0V'
          'why': 'src/rnsh.cpp processGate(): triggers[n].process(rescale(getVoltage(), 0.1f, 2.f, 0.f, 1.f)) — one voltage read per frame, no channel loop'
        '8':
          'poly': false
          'range': '0 to 10V'
          'step': 'continuous'
          'negative': 'swings'
          'normal': 'the Spread knob'
          'why': 'src/rnsh.cpp process(): if (inputs[INPUT1_INPUT].isConnected()) spread = getVoltage() / 10; else spread = params[PARAM1_PARAM].getValue()'
      'out':
        '0':
          'poly': false
          'why': 'src/rnsh.cpp outputNoise(): setVoltage(random::normal() * 5.f/sqrt(2.f) * spread), one voltage, no channel count set'
        '1':
          'poly': false
          'why': 'src/rnsh.cpp outputNoise(): setVoltage(random::normal() * 5.f/sqrt(2.f) * spread), one voltage, no channel count set'
        '2':
          'poly': false
          'why': 'src/rnsh.cpp outputNoise(): setVoltage(random::normal() * 5.f/sqrt(2.f) * spread), one voltage, no channel count set'
        '3':
          'poly': false
          'why': 'src/rnsh.cpp outputNoise(): setVoltage(random::normal() * 5.f/sqrt(2.f) * spread), one voltage, no channel count set'
        '4':
          'poly': false
          'why': 'src/rnsh.cpp outputNoise(): setVoltage(random::normal() * 5.f/sqrt(2.f) * spread), one voltage, no channel count set'
        '5':
          'poly': false
          'why': 'src/rnsh.cpp outputNoise(): setVoltage(random::normal() * 5.f/sqrt(2.f) * spread), one voltage, no channel count set'
        '6':
          'poly': false
          'why': 'src/rnsh.cpp outputNoise(): setVoltage(random::normal() * 5.f/sqrt(2.f) * spread), one voltage, no channel count set'
        '7':
          'poly': false
          'why': 'src/rnsh.cpp outputNoise(): setVoltage(random::normal() * 5.f/sqrt(2.f) * spread), one voltage, no channel count set'
    'notes': 'No family is given for the eight Out jacks: with its Gate jack unpatched a jack carries audio-rate noise, and with a gate patched it carries a stepped random control voltage, so one colour would be wrong half the time. The CV input is divided by ten and used directly, with no clamp, so voltages above 10V scale the outputs past the knob''s own maximum and negative voltages invert the noise; the maker''s port name calls it a 0 to 10V input. The output gain is 5 divided by the square root of 2 applied to a normally distributed sample, so the swing is not hard-bounded at ±5V.'
