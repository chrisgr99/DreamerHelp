{
  "plugin": "DHE-Modules",
  "module": "Stage",
  "facts": {
    "inputs": {
      "0": {"family": "cv", "poly": false, "step": "continuous", "why": "DHE-Modules 2.0.3 src/modules/envelope/stage/signals.h: inputs[Envelope] is read with getVoltage() and used as the signal; the plugin never calls getPolyVoltage or getChannels"},
      "1": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/envelope/stage/signals.h: is_high(inputs[Trigger]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"},
      "2": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/envelope/stage/signals.h: is_high(inputs[Defer]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"}
    },
    "outputs": {
      "0": {"family": "cv"},
      "1": {"family": "trigger", "poly": false, "range": "0 to 10V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/envelope/stage/signals.h: outputs[Eoc] is set from UnipolarVoltage::scale of a bool, so 0V or 10V; the plugin never calls getPolyVoltage or getChannels"},
      "2": {"family": "trigger", "poly": false, "range": "0 to 10V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/envelope/stage/signals.h: outputs[Active] is set from UnipolarVoltage::scale of a bool, so 0V or 10V; the plugin never calls getPolyVoltage or getChannels"}
    }
  }
}
