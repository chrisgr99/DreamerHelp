{
  "plugin": "DHE-Modules",
  "module": "CurveSequencer16",
  "facts": {
    "inputs": {
      "0": {"family": "cv", "poly": false, "step": "continuous", "why": "DHE-Modules 2.0.3 src/modules/curve-sequencer/signals.h: inputs[Main] is read with getVoltage() and used as the signal; the plugin never calls getPolyVoltage or getChannels"},
      "1": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/curve-sequencer/signals.h: is_high(inputs[Gate]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"},
      "2": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/curve-sequencer/signals.h: is_high(inputs[Loop]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"},
      "3": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/curve-sequencer/signals.h: is_high(inputs[Reset]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"},
      "4": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/curve-sequencer/signals.h: is_high(inputs[Run]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"},
      "5": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/curve-sequencer/signals.h: is_high(inputs[StepEnabled + 0]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"},
      "6": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/curve-sequencer/signals.h: is_high(inputs[StepEnabled + 1]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"},
      "7": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/curve-sequencer/signals.h: is_high(inputs[StepEnabled + 2]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"},
      "8": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/curve-sequencer/signals.h: is_high(inputs[StepEnabled + 3]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"},
      "9": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/curve-sequencer/signals.h: is_high(inputs[StepEnabled + 4]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"},
      "10": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/curve-sequencer/signals.h: is_high(inputs[StepEnabled + 5]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"},
      "11": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/curve-sequencer/signals.h: is_high(inputs[StepEnabled + 6]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"},
      "12": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/curve-sequencer/signals.h: is_high(inputs[StepEnabled + 7]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"},
      "13": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/curve-sequencer/signals.h: is_high(inputs[StepEnabled + 8]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"},
      "14": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/curve-sequencer/signals.h: is_high(inputs[StepEnabled + 9]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"},
      "15": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/curve-sequencer/signals.h: is_high(inputs[StepEnabled + 10]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"},
      "16": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/curve-sequencer/signals.h: is_high(inputs[StepEnabled + 11]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"},
      "17": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/curve-sequencer/signals.h: is_high(inputs[StepEnabled + 12]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"},
      "18": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/curve-sequencer/signals.h: is_high(inputs[StepEnabled + 13]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"},
      "19": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/curve-sequencer/signals.h: is_high(inputs[StepEnabled + 14]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"},
      "20": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/curve-sequencer/signals.h: is_high(inputs[StepEnabled + 15]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"}
    },
    "outputs": {
      "0": {"family": "cv"}
    }
  },
  "comment": "The last parameter has no name and no control on the panel: the source calls it GenerateModeMenu and nothing reads it, so it is left untagged."
}
