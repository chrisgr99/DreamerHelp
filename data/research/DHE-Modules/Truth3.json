{
  "plugin": "DHE-Modules",
  "module": "Truth3",
  "facts": {
    "inputs": {
      "0": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/truth/module.h:109: is_high(inputs[Input + 0]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"},
      "1": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/truth/module.h:109: is_high(inputs[Input + 1]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"},
      "2": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/truth/module.h:109: is_high(inputs[Input + 2]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"}
    },
    "outputs": {
      "0": {"family": "trigger", "poly": false, "range": "0 to 10V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/truth/module.h:54: this output is set to 10.F or 0.F; the plugin never calls getPolyVoltage or getChannels"},
      "1": {"family": "trigger", "poly": false, "range": "0 to 10V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/truth/module.h:54: this output is set to 10.F or 0.F; the plugin never calls getPolyVoltage or getChannels"}
    }
  },
  "comment": "The maker's manual says an input above 0.5V counts as high; the 2.0.3 source reads an input as high above 1V. The lines follow the source."
}
