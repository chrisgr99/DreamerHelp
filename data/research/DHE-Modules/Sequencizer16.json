{
  "plugin": "DHE-Modules",
  "module": "Sequencizer16",
  "facts": {
    "inputs": {
      "0": {"family": "cv", "poly": false, "polarity": "unipolar", "step": "continuous", "why": "DHE-Modules 2.0.3 src/modules/sequencizer/signals.h: inputs[DurationMultiplierCV] goes through modulation_of(), which multiplies getVoltage() by 0.1 into a 0 to 1 knob rotation (src/signals/basic.h:24); the plugin never calls getPolyVoltage or getChannels"},
      "1": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/sequencizer/signals.h: is_high(inputs[Gate]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"},
      "2": {"family": "cv", "poly": false, "step": "continuous", "why": "DHE-Modules 2.0.3 src/modules/sequencizer/signals.h: inputs[InA] is read with getVoltage() and used as the signal; the plugin never calls getPolyVoltage or getChannels"},
      "3": {"family": "cv", "poly": false, "step": "continuous", "why": "DHE-Modules 2.0.3 src/modules/sequencizer/signals.h: inputs[InB] is read with getVoltage() and used as the signal; the plugin never calls getPolyVoltage or getChannels"},
      "4": {"family": "cv", "poly": false, "step": "continuous", "why": "DHE-Modules 2.0.3 src/modules/sequencizer/signals.h: inputs[InC] is read with getVoltage() and used as the signal; the plugin never calls getPolyVoltage or getChannels"},
      "5": {"family": "cv", "poly": false, "polarity": "unipolar", "step": "continuous", "why": "DHE-Modules 2.0.3 src/modules/sequencizer/signals.h: inputs[LevelAttenuationCV] goes through modulation_of(), which multiplies getVoltage() by 0.1 into a 0 to 1 knob rotation (src/signals/basic.h:24); the plugin never calls getPolyVoltage or getChannels"},
      "6": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/sequencizer/signals.h: is_high(inputs[Loop]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"},
      "7": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/sequencizer/signals.h: is_high(inputs[Reset]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"},
      "8": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "DHE-Modules 2.0.3 src/modules/sequencizer/signals.h: is_high(inputs[Run]) compares getVoltage() with 1.0 (src/signals/basic.h:20); the plugin never calls getPolyVoltage or getChannels"}
    },
    "outputs": {
      "0": {"family": "trigger"},
      "1": {"family": "trigger"},
      "2": {"family": "cv"},
      "3": {"family": "trigger"},
      "4": {"family": "trigger"},
      "5": {"family": "cv"}
    }
  },
  "comment": "The maker's manual says the STEP delta and SEQ delta outputs emit a 10ms pulse; the 2.0.3 source sets the pulse length to 0.9ms. The lines follow the source."
}
