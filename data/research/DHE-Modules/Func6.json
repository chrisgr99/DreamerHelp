{
  "plugin": "DHE-Modules",
  "module": "Func6",
  "facts": {
    "inputs": {
      "0": {"poly": false, "step": "continuous", "why": "DHE-Modules 2.0.3 src/modules/func/signals.h:28: getNormalVoltage() on this port, used as the signal; the plugin never calls getPolyVoltage or getChannels"},
      "1": {"poly": false, "step": "continuous", "why": "DHE-Modules 2.0.3 src/modules/func/signals.h:28: getNormalVoltage() on this port, used as the signal; the plugin never calls getPolyVoltage or getChannels"},
      "2": {"poly": false, "step": "continuous", "why": "DHE-Modules 2.0.3 src/modules/func/signals.h:28: getNormalVoltage() on this port, used as the signal; the plugin never calls getPolyVoltage or getChannels"},
      "3": {"poly": false, "step": "continuous", "why": "DHE-Modules 2.0.3 src/modules/func/signals.h:28: getNormalVoltage() on this port, used as the signal; the plugin never calls getPolyVoltage or getChannels"},
      "4": {"poly": false, "step": "continuous", "why": "DHE-Modules 2.0.3 src/modules/func/signals.h:28: getNormalVoltage() on this port, used as the signal; the plugin never calls getPolyVoltage or getChannels"},
      "5": {"poly": false, "step": "continuous", "why": "DHE-Modules 2.0.3 src/modules/func/signals.h:28: getNormalVoltage() on this port, used as the signal; the plugin never calls getPolyVoltage or getChannels"}
    }
  },
  "comment": "No family for the channel jacks: Func 6 offsets or scales whatever it is given, so what those jacks carry depends on the patch."
}
