{
  "plugin": "DHE-Modules",
  "module": "Swave",
  "facts": {
    "inputs": {
      "0": {"family": "cv", "poly": false, "polarity": "unipolar", "step": "continuous", "why": "DHE-Modules 2.0.3 src/modules/swave/module.h: inputs[CurvatureCv] goes through modulation_of(), which multiplies getVoltage() by 0.1 into a 0 to 1 knob rotation (src/signals/basic.h:24); the plugin never calls getPolyVoltage or getChannels"},
      "1": {"poly": false, "step": "continuous", "why": "DHE-Modules 2.0.3 src/modules/swave/module.h: inputs[Swave] is read with getVoltage() and used as the signal; the plugin never calls getPolyVoltage or getChannels"}
    }
  },
  "comment": "No family for the IN and OUT jacks: Swave shapes whatever it is given, audio or modulation, so what those jacks carry depends on the patch."
}
