{
  "plugin": "DHE-Modules",
  "module": "Ranger",
  "facts": {
    "inputs": {
      "0": {"family": "cv", "poly": false, "polarity": "unipolar", "step": "continuous", "why": "DHE-Modules 2.0.3 src/modules/ranger/module.h: inputs[LevelCv] goes through modulation_of(), which multiplies getVoltage() by 0.1 into a 0 to 1 knob rotation (src/signals/basic.h:24); the plugin never calls getPolyVoltage or getChannels"},
      "1": {"family": "cv", "poly": false, "polarity": "unipolar", "step": "continuous", "why": "DHE-Modules 2.0.3 src/modules/ranger/module.h: inputs[CcwLimitCv] goes through modulation_of(), which multiplies getVoltage() by 0.1 into a 0 to 1 knob rotation (src/signals/basic.h:24); the plugin never calls getPolyVoltage or getChannels"},
      "2": {"family": "cv", "poly": false, "polarity": "unipolar", "step": "continuous", "why": "DHE-Modules 2.0.3 src/modules/ranger/module.h: inputs[CwLimitCv] goes through modulation_of(), which multiplies getVoltage() by 0.1 into a 0 to 1 knob rotation (src/signals/basic.h:24); the plugin never calls getPolyVoltage or getChannels"}
    },
    "outputs": {
      "0": {"family": "cv"}
    }
  }
}
