{
  "plugin": "Sulamith",
  "module": "VoltM",
  "facts": {
    "inputs": {
      "0": {"poly": true, "step": "continuous", "why": "VoltM.cpp VoltM::process: getChannels() on each of the three jacks is summed and clamped to 16 to set the output's channel count, and getVoltage(i) reads each channel into the display array, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "1": {"poly": true, "step": "continuous", "why": "VoltM.cpp VoltM::process: getChannels() on each of the three jacks is summed and clamped to 16 to set the output's channel count, and getVoltage(i) reads each channel into the display array, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "2": {"poly": true, "step": "continuous", "why": "VoltM.cpp VoltM::process: getChannels() on each of the three jacks is summed and clamped to 16 to set the output's channel count, and getVoltage(i) reads each channel into the display array, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"}
    },
    "outputs": {
      "0": {"poly": true, "step": "continuous", "why": "VoltM.cpp VoltM::process: setChannels(chmix) then setVoltage(valLines[i], i) for each channel, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"}
    }
  },
  "comment": "The jacks carry whatever is patched to them, so no family is set. The display string is rebuilt once every 2050 samples."
}
