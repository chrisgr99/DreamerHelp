{
  "plugin": "Sulamith",
  "module": "Split",
  "facts": {
    "inputs": {
      "0": {"poly": true, "step": "continuous", "why": "Split.cpp Split::process: getChannels() on this jack lights the lamps and getVoltage(c) is read for all sixteen channel positions in the loop, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"}
    },
    "outputs": {
      "0": {"poly": false, "step": "continuous", "why": "Split.cpp Split::process: setVoltage of the input's channel c with no channel argument, so every output is monophonic; Rack returns 0V for a channel the input does not have, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "1": {"poly": false, "step": "continuous", "why": "Split.cpp Split::process: setVoltage of the input's channel c with no channel argument, so every output is monophonic; Rack returns 0V for a channel the input does not have, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "2": {"poly": false, "step": "continuous", "why": "Split.cpp Split::process: setVoltage of the input's channel c with no channel argument, so every output is monophonic; Rack returns 0V for a channel the input does not have, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "3": {"poly": false, "step": "continuous", "why": "Split.cpp Split::process: setVoltage of the input's channel c with no channel argument, so every output is monophonic; Rack returns 0V for a channel the input does not have, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "4": {"poly": false, "step": "continuous", "why": "Split.cpp Split::process: setVoltage of the input's channel c with no channel argument, so every output is monophonic; Rack returns 0V for a channel the input does not have, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "5": {"poly": false, "step": "continuous", "why": "Split.cpp Split::process: setVoltage of the input's channel c with no channel argument, so every output is monophonic; Rack returns 0V for a channel the input does not have, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "6": {"poly": false, "step": "continuous", "why": "Split.cpp Split::process: setVoltage of the input's channel c with no channel argument, so every output is monophonic; Rack returns 0V for a channel the input does not have, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "7": {"poly": false, "step": "continuous", "why": "Split.cpp Split::process: setVoltage of the input's channel c with no channel argument, so every output is monophonic; Rack returns 0V for a channel the input does not have, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "8": {"poly": false, "step": "continuous", "why": "Split.cpp Split::process: setVoltage of the input's channel c with no channel argument, so every output is monophonic; Rack returns 0V for a channel the input does not have, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "9": {"poly": false, "step": "continuous", "why": "Split.cpp Split::process: setVoltage of the input's channel c with no channel argument, so every output is monophonic; Rack returns 0V for a channel the input does not have, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "10": {"poly": false, "step": "continuous", "why": "Split.cpp Split::process: setVoltage of the input's channel c with no channel argument, so every output is monophonic; Rack returns 0V for a channel the input does not have, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "11": {"poly": false, "step": "continuous", "why": "Split.cpp Split::process: setVoltage of the input's channel c with no channel argument, so every output is monophonic; Rack returns 0V for a channel the input does not have, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "12": {"poly": false, "step": "continuous", "why": "Split.cpp Split::process: setVoltage of the input's channel c with no channel argument, so every output is monophonic; Rack returns 0V for a channel the input does not have, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "13": {"poly": false, "step": "continuous", "why": "Split.cpp Split::process: setVoltage of the input's channel c with no channel argument, so every output is monophonic; Rack returns 0V for a channel the input does not have, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "14": {"poly": false, "step": "continuous", "why": "Split.cpp Split::process: setVoltage of the input's channel c with no channel argument, so every output is monophonic; Rack returns 0V for a channel the input does not have, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "15": {"poly": false, "step": "continuous", "why": "Split.cpp Split::process: setVoltage of the input's channel c with no channel argument, so every output is monophonic; Rack returns 0V for a channel the input does not have, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"}
    }
  },
  "comment": "The jacks carry whatever is patched to them, so no family is set. All sixteen outputs are written every sample whatever the input's channel count, so the unused ones read 0V rather than holding a stale value."
}
