{
  "plugin": "Sulamith",
  "module": "Merge",
  "facts": {
    "inputs": {
      "0": {"poly": true, "step": "continuous", "why": "Merge.cpp Merge::process: getChannels() on each jack in the default branch and getVoltage(c) for each of its channels, packed into the output at a running count, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "1": {"poly": true, "step": "continuous", "why": "Merge.cpp Merge::process: getChannels() on each jack in the default branch and getVoltage(c) for each of its channels, packed into the output at a running count, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "2": {"poly": true, "step": "continuous", "why": "Merge.cpp Merge::process: getChannels() on each jack in the default branch and getVoltage(c) for each of its channels, packed into the output at a running count, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "3": {"poly": true, "step": "continuous", "why": "Merge.cpp Merge::process: getChannels() on each jack in the default branch and getVoltage(c) for each of its channels, packed into the output at a running count, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "4": {"poly": true, "step": "continuous", "why": "Merge.cpp Merge::process: getChannels() on each jack in the default branch and getVoltage(c) for each of its channels, packed into the output at a running count, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "5": {"poly": true, "step": "continuous", "why": "Merge.cpp Merge::process: getChannels() on each jack in the default branch and getVoltage(c) for each of its channels, packed into the output at a running count, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "6": {"poly": true, "step": "continuous", "why": "Merge.cpp Merge::process: getChannels() on each jack in the default branch and getVoltage(c) for each of its channels, packed into the output at a running count, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "7": {"poly": true, "step": "continuous", "why": "Merge.cpp Merge::process: getChannels() on each jack in the default branch and getVoltage(c) for each of its channels, packed into the output at a running count, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "8": {"poly": true, "step": "continuous", "why": "Merge.cpp Merge::process: getChannels() on each jack in the default branch and getVoltage(c) for each of its channels, packed into the output at a running count, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "9": {"poly": true, "step": "continuous", "why": "Merge.cpp Merge::process: getChannels() on each jack in the default branch and getVoltage(c) for each of its channels, packed into the output at a running count, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "10": {"poly": true, "step": "continuous", "why": "Merge.cpp Merge::process: getChannels() on each jack in the default branch and getVoltage(c) for each of its channels, packed into the output at a running count, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "11": {"poly": true, "step": "continuous", "why": "Merge.cpp Merge::process: getChannels() on each jack in the default branch and getVoltage(c) for each of its channels, packed into the output at a running count, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "12": {"poly": true, "step": "continuous", "why": "Merge.cpp Merge::process: getChannels() on each jack in the default branch and getVoltage(c) for each of its channels, packed into the output at a running count, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "13": {"poly": true, "step": "continuous", "why": "Merge.cpp Merge::process: getChannels() on each jack in the default branch and getVoltage(c) for each of its channels, packed into the output at a running count, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "14": {"poly": true, "step": "continuous", "why": "Merge.cpp Merge::process: getChannels() on each jack in the default branch and getVoltage(c) for each of its channels, packed into the output at a running count, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"},
      "15": {"poly": true, "step": "continuous", "why": "Merge.cpp Merge::process: getChannels() on each jack in the default branch and getVoltage(c) for each of its channels, packed into the output at a running count, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"}
    },
    "outputs": {
      "0": {"poly": true, "step": "continuous", "why": "Merge.cpp Merge::process: setVoltage(volt, ch_sum) for each gathered channel, with outputs[mOUT].channels set to the running total capped at 16, commit 086d8c4, whose plugin.json carries the installed 2.3.2 and whose res/ matches the installed panels"}
    }
  },
  "comment": "The jacks carry whatever is patched to them, so no family is set. In the monophonic branch the voltage is written at setVoltage(volt, n) where n is the jack index, while the channel count is the number of connected jacks, so with a gap in the patching the written positions run past the reported count."
}
