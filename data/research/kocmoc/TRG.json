{
  "plugin": "kocmoc",
  "module": "TRG",
  "facts": {
    "inputs": {
      "0": {"family": "trigger", "poly": false, "range": "high above 0.5V", "step": "stepped", "negative": "ignored", "why": "TRG.cpp:89 getVoltage() > 0.5f advances, :109 below 0.5f drops the gate"},
      "1": {"family": "trigger", "poly": false, "range": "high above 0.5V", "step": "stepped", "negative": "ignored", "why": "TRG.cpp:115 getVoltage() > 0.5f resets the step to zero"}
    },
    "outputs": {
      "0": {"family": "trigger", "poly": false, "range": "0 to 10V", "why": "TRG.cpp:138 setVoltage(gate_state * 10.f) with no channel index and no setChannels call"}
    }
  },
  "comment": "The LEN knob is the only parameter the installed build declares, which is how this build was placed in the maker's history: a later commit, 300e98f, replaces the hand-drawn grid with real ParamWidgets and adds thirty-two Step parameters and a Page parameter. The census shows one parameter, so the installed build predates that change. Randomising the module fills the grid at random (TRG.cpp onRandomize), which no control or menu item announces."
}
