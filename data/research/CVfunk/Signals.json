{
  "plugin": "CVfunk",
  "module": "Signals",
  "facts": {
    "inputs": {
      "0": {"poly": true, "step": "continuous", "range": "±10V", "negative": "swings", "why": "Signals.cpp:144 takes getChannels() on this input and Signals.cpp:200 reads it with getPolyVoltage; Signals.cpp:200 clamps it to -10 to 10V"},
      "1": {"poly": true, "step": "continuous", "range": "±10V", "negative": "swings", "normal": "the jack above it", "why": "Signals.cpp:144 takes getChannels() on this input; Signals.cpp:145 falls back to the nearest patched row above when nothing is patched"},
      "2": {"poly": true, "step": "continuous", "range": "±10V", "negative": "swings", "normal": "the jack above it", "why": "Signals.cpp:144 takes getChannels() on this input; Signals.cpp:145 falls back to the nearest patched row above when nothing is patched"},
      "3": {"poly": true, "step": "continuous", "range": "±10V", "negative": "swings", "normal": "the jack above it", "why": "Signals.cpp:144 takes getChannels() on this input; Signals.cpp:145 falls back to the nearest patched row above when nothing is patched"},
      "4": {"poly": true, "step": "continuous", "range": "±10V", "negative": "swings", "normal": "the jack above it", "why": "Signals.cpp:144 takes getChannels() on this input; Signals.cpp:145 falls back to the nearest patched row above when nothing is patched"},
      "5": {"poly": true, "step": "continuous", "range": "±10V", "negative": "swings", "normal": "the jack above it", "why": "Signals.cpp:144 takes getChannels() on this input; Signals.cpp:145 falls back to the nearest patched row above when nothing is patched"}
    },
    "outputs": {
      "0": {"poly": false, "range": "±10V", "why": "Signals.cpp:248 writes one channel with setVoltage from the clamped input value"}
    }
  },
  "comment": "No family is given for these jacks: the module passes through whatever is patched, audio or CV alike, so a colour would be a claim the module cannot make. The maker left all twelve port names blank, so Rack shows no tooltip on them."
}
