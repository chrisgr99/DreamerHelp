{
  "plugin": "MockbaModular",
  "module": "DualAND",
  "facts": {
    "inputs": {
      "0": {"family": "trigger"},
      "1": {"family": "trigger"},
      "2": {"family": "trigger"},
      "3": {"family": "trigger"}
    },
    "outputs": {
      "0": {"family": "trigger"},
      "1": {"family": "trigger"}
    }
  },
  "comment": "Thresholds read from the process() of the installed 2.0.1 source: the comparison is getVoltage() > 0.0, not the 1V an earlier draft of this entry gave. All jacks are read with getVoltage(), which is channel 0, so the module is mono."
}
