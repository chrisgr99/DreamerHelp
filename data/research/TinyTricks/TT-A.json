{
  "plugin": "TinyTricks",
  "module": "TT-A",
  "facts": {
    "inputs": {
      "0": {"poly": true, "why": "arithmetic.cpp:53 nChan = max(1, inputs[A].getChannels()); :59 getVoltage(c) inside that loop"},
      "1": {"poly": true, "why": "arithmetic.cpp:60 getPolyVoltage(c)"}
    },
    "outputs": {
      "0": {"poly": true, "why": "arithmetic.cpp:54-55 setChannels(nChan) on every output, nChan taken from the A input"},
      "1": {"poly": true, "why": "arithmetic.cpp:54-55 setChannels(nChan) on every output, nChan taken from the A input"},
      "2": {"poly": true, "why": "arithmetic.cpp:54-55 setChannels(nChan) on every output, nChan taken from the A input"},
      "3": {"poly": true, "why": "arithmetic.cpp:54-55 setChannels(nChan) on every output, nChan taken from the A input"},
      "4": {"poly": true, "why": "arithmetic.cpp:54-55 setChannels(nChan) on every output, nChan taken from the A input"},
      "5": {"poly": true, "why": "arithmetic.cpp:54-55 setChannels(nChan) on every output, nChan taken from the A input"},
      "6": {"poly": true, "why": "arithmetic.cpp:54-55 setChannels(nChan) on every output, nChan taken from the A input"},
      "7": {"poly": true, "why": "arithmetic.cpp:54-55 setChannels(nChan) on every output, nChan taken from the A input"},
      "8": {"poly": true, "why": "arithmetic.cpp:54-55 setChannels(nChan) on every output, nChan taken from the A input"},
      "9": {"poly": true, "why": "arithmetic.cpp:54-55 setChannels(nChan) on every output, nChan taken from the A input"},
      "10": {"poly": true, "why": "arithmetic.cpp:54-55 setChannels(nChan) on every output, nChan taken from the A input"},
      "11": {"poly": true, "why": "arithmetic.cpp:54-55 setChannels(nChan) on every output, nChan taken from the A input"}
    }
  },
  "comment": "No family is given for the two inputs or the twelve outputs: the module does arithmetic on whatever voltage is patched, so the meaning comes from the cable."
}
