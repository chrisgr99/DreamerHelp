{
  "plugin": "ZetaCarinae",
  "module": "OrnsteinUhlenbeck",
  "facts": {
    "inputs": {
      "0": {"family": "cv", "poly": true, "step": "continuous", "negative": "subtracts", "why": "OrnsteinUhlenbeck.cpp OrnsteinUhlenbeck::process: the jack is added to its knob and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "1": {"family": "cv", "poly": true, "step": "continuous", "negative": "subtracts", "why": "OrnsteinUhlenbeck.cpp OrnsteinUhlenbeck::process: the jack is added to its knob and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "2": {"family": "cv", "poly": true, "step": "continuous", "negative": "subtracts", "why": "OrnsteinUhlenbeck.cpp OrnsteinUhlenbeck::process: the jack is added to its knob and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "3": {"family": "trigger", "poly": true, "range": "high above 1V", "step": "stepped", "why": "OrnsteinUhlenbeck.cpp OrnsteinUhlenbeck::process: a Rack SchmittTrigger at its default thresholds, per channel, commit 54dbe9b2, the commit that set the version to 2.0.4"}
    },
    "outputs": {
      "0": {"family": "cv", "poly": true, "step": "continuous", "why": "OrnsteinUhlenbeck.cpp OrnsteinUhlenbeck::process: setChannels is called with the widest input channel count, commit 54dbe9b2, the commit that set the version to 2.0.4"}
    }
  },
  "comment": "None of this module's jacks carries a name in the installed build: the maker added input and output tooltips in the commit after this one, so hovering a jack here shows nothing."
}
