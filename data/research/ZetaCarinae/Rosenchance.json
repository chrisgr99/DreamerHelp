{
  "plugin": "ZetaCarinae",
  "module": "Rosenchance",
  "facts": {
    "inputs": {
      "0": {"family": "trigger", "poly": true, "range": "high above 1V", "step": "stepped", "why": "Rosenchance.cpp Rosenchance::process: a Rack SchmittTrigger at its default thresholds, per channel, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "1": {"family": "cv", "poly": true, "step": "continuous", "negative": "subtracts", "why": "Rosenchance.cpp Rosenchance::process: the jack is scaled by its attenuator knob and added to its main knob, and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "2": {"family": "cv", "poly": true, "step": "continuous", "negative": "subtracts", "why": "Rosenchance.cpp Rosenchance::process: the jack is scaled by its attenuator knob and added to its main knob, and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "3": {"family": "cv", "poly": true, "step": "continuous", "negative": "subtracts", "why": "Rosenchance.cpp Rosenchance::process: the jack is scaled by its attenuator knob and added to its main knob, and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "4": {"family": "cv", "poly": true, "step": "continuous", "negative": "subtracts", "why": "Rosenchance.cpp Rosenchance::process: the jack is scaled by its attenuator knob and added to its main knob, and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "5": {"family": "cv", "poly": true, "step": "continuous", "negative": "subtracts", "why": "Rosenchance.cpp Rosenchance::process: the jack is scaled by its attenuator knob and added to its main knob, and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "6": {"family": "cv", "poly": true, "step": "continuous", "negative": "subtracts", "why": "Rosenchance.cpp Rosenchance::process: the jack is scaled by its attenuator knob and added to its main knob, and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "7": {"family": "cv", "poly": true, "step": "continuous", "negative": "subtracts", "why": "Rosenchance.cpp Rosenchance::process: the jack is scaled by its attenuator knob and added to its main knob, and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "8": {"family": "cv", "poly": true, "step": "continuous", "negative": "subtracts", "why": "Rosenchance.cpp Rosenchance::process: the jack is scaled by its attenuator knob and added to its main knob, and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"}
    },
    "outputs": {
      "0": {"family": "cv", "poly": true, "range": "±10V", "step": "stepped", "why": "Rosenchance.cpp Rosenchance::process: the chosen emission knob value, which runs -10V to 10V, is written per channel, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "1": {"family": "cv", "poly": true, "range": "±10V", "step": "stepped", "why": "Rosenchance.cpp Rosenchance::process: the chosen emission knob value, which runs -10V to 10V, is written per channel, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "2": {"family": "trigger", "poly": true, "range": "0 to 5V", "step": "stepped", "why": "Rosenchance.cpp Rosenchance::process: the two state outputs are written as 5V or 0V, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "3": {"family": "trigger", "poly": true, "range": "0 to 5V", "step": "stepped", "why": "Rosenchance.cpp Rosenchance::process: the two state outputs are written as 5V or 0V, commit 54dbe9b2, the commit that set the version to 2.0.4"}
    }
  },
  "comment": "None of this module's jacks carries a name in the installed build: the maker added input and output tooltips in the commit after this one, so hovering a jack here shows nothing. The counter that clears the two state outputs is shared across polyphonic channels rather than kept per channel, so with several channels running the pulses are shorter than with one."
}
