{
  "plugin": "ZetaCarinae",
  "module": "BrownianBridge",
  "facts": {
    "inputs": {
      "0": {"family": "cv", "poly": true, "step": "continuous", "negative": "subtracts", "why": "BrownianBridge.cpp BrownianBridge::process: the jack is added to its knob and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "1": {"family": "cv", "poly": true, "step": "continuous", "negative": "subtracts", "why": "BrownianBridge.cpp BrownianBridge::process: the jack is added to its knob and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "2": {"family": "cv", "poly": true, "step": "continuous", "negative": "subtracts", "why": "BrownianBridge.cpp BrownianBridge::process: the jack is added to its knob and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "3": {"family": "cv", "poly": true, "step": "continuous", "negative": "subtracts", "why": "BrownianBridge.cpp BrownianBridge::process: the jack is added to its knob and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "4": {"family": "trigger", "poly": true, "range": "high above 1V", "step": "stepped", "why": "BrownianBridge.cpp BrownianBridge::process: a Rack SchmittTrigger at its default thresholds, per channel, commit 54dbe9b2, the commit that set the version to 2.0.4"}
    },
    "outputs": {
      "0": {"family": "cv", "poly": true, "step": "continuous", "why": "BrownianBridge.cpp BrownianBridge::process: setChannels is called with the widest input channel count, commit 54dbe9b2, the commit that set the version to 2.0.4"}
    }
  },
  "comment": "None of this module's jacks carries a name in the installed build: the maker added input and output tooltips in the commit after this one, so hovering a jack here shows nothing. The walk is restarted whenever the computed total time differs from the one in use, which means any movement of the time knob or the time jack retriggers it."
}
