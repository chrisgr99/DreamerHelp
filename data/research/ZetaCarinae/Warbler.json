{
  "plugin": "ZetaCarinae",
  "module": "Warbler",
  "facts": {
    "inputs": {
      "0": {"family": "cv", "poly": true, "step": "continuous", "negative": "subtracts", "why": "Warbler.cpp WarblerModule::process: the jack is scaled by its attenuator knob and added to its main knob, and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "1": {"family": "cv", "poly": true, "step": "continuous", "negative": "subtracts", "why": "Warbler.cpp WarblerModule::process: the jack is scaled by its attenuator knob and added to its main knob, and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "2": {"family": "cv", "poly": true, "step": "continuous", "negative": "subtracts", "why": "Warbler.cpp WarblerModule::process: the jack is scaled by its attenuator knob and added to its main knob, and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "3": {"family": "cv", "poly": true, "step": "continuous", "negative": "subtracts", "why": "Warbler.cpp WarblerModule::process: the jack is scaled by its attenuator knob and added to its main knob, and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "4": {"family": "pitch", "poly": true, "range": "1V per octave", "step": "continuous", "why": "Warbler.cpp WarblerModule::process: the jack is put through FREQ_C4 times 2 to the power of the voltage, and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "5": {"poly": true, "range": "±10V", "step": "continuous", "why": "Warbler.cpp WarblerModule::process: the jack is divided by 10 and driven into the systems, and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"}
    },
    "outputs": {
      "0": {"family": "audio", "poly": true, "range": "±5V", "step": "continuous", "why": "Warbler.cpp WarblerModule::process: the summed coordinates are halved and clamped to ±5V, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "1": {"family": "audio", "poly": true, "range": "±5V", "step": "continuous", "why": "Warbler.cpp WarblerModule::process: the summed coordinates are halved and clamped to ±5V, commit 54dbe9b2, the commit that set the version to 2.0.4"}
    }
  },
  "comment": "None of this module's jacks carries a name in the installed build: the maker added input and output tooltips in the commit after this one, so hovering a jack here shows nothing. The harmonics knob indexes a table of twenty-one rows of eight multipliers; the module's own tooltip calls it (Sub)Harmonics."
}
