{
  "plugin": "ZetaCarinae",
  "module": "GuildensTurn",
  "facts": {
    "inputs": {
      "0": {"family": "trigger", "poly": true, "range": "high above 1V", "step": "stepped", "why": "GuildensTurn.cpp GuildensTurn::process: a Rack SchmittTrigger at its default thresholds, per channel, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "1": {"poly": true, "step": "continuous", "why": "GuildensTurn.cpp GuildensTurn::process: the selected state input is copied straight to the output, and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "2": {"poly": true, "step": "continuous", "why": "GuildensTurn.cpp GuildensTurn::process: the selected state input is copied straight to the output, and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "3": {"poly": true, "step": "continuous", "why": "GuildensTurn.cpp GuildensTurn::process: the selected state input is copied straight to the output, and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "4": {"poly": true, "step": "continuous", "why": "GuildensTurn.cpp GuildensTurn::process: the selected state input is copied straight to the output, and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "5": {"family": "cv", "poly": true, "range": "0 to 1V", "step": "continuous", "negative": "subtracts", "why": "GuildensTurn.cpp GuildensTurn::process: the jack is scaled by its attenuator and added to a probability knob that is clamped to 0 to 1, and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "6": {"family": "cv", "poly": true, "range": "0 to 1V", "step": "continuous", "negative": "subtracts", "why": "GuildensTurn.cpp GuildensTurn::process: the jack is scaled by its attenuator and added to a probability knob that is clamped to 0 to 1, and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "7": {"family": "cv", "poly": true, "range": "0 to 1V", "step": "continuous", "negative": "subtracts", "why": "GuildensTurn.cpp GuildensTurn::process: the jack is scaled by its attenuator and added to a probability knob that is clamped to 0 to 1, and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "8": {"family": "cv", "poly": true, "range": "0 to 1V", "step": "continuous", "negative": "subtracts", "why": "GuildensTurn.cpp GuildensTurn::process: the jack is scaled by its attenuator and added to a probability knob that is clamped to 0 to 1, and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "9": {"family": "cv", "poly": true, "range": "0 to 1V", "step": "continuous", "negative": "subtracts", "why": "GuildensTurn.cpp GuildensTurn::process: the jack is scaled by its attenuator and added to a probability knob that is clamped to 0 to 1, and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "10": {"family": "cv", "poly": true, "range": "0 to 1V", "step": "continuous", "negative": "subtracts", "why": "GuildensTurn.cpp GuildensTurn::process: the jack is scaled by its attenuator and added to a probability knob that is clamped to 0 to 1, and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "11": {"family": "cv", "poly": true, "range": "0 to 1V", "step": "continuous", "negative": "subtracts", "why": "GuildensTurn.cpp GuildensTurn::process: the jack is scaled by its attenuator and added to a probability knob that is clamped to 0 to 1, and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "12": {"family": "cv", "poly": true, "range": "0 to 1V", "step": "continuous", "negative": "subtracts", "why": "GuildensTurn.cpp GuildensTurn::process: the jack is scaled by its attenuator and added to a probability knob that is clamped to 0 to 1, and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"}
    },
    "outputs": {
      "0": {"poly": true, "step": "continuous", "why": "GuildensTurn.cpp GuildensTurn::process: the selected state input is copied straight through, setChannels with the widest input count, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "1": {"family": "cv", "poly": true, "range": "1 to 4V", "step": "stepped", "why": "GuildensTurn.cpp GuildensTurn::process: the state number, 1 to 4, is written per channel, commit 54dbe9b2, the commit that set the version to 2.0.4"}
    }
  },
  "comment": "None of this module's jacks carries a name in the installed build: the maker added input and output tooltips in the commit after this one, so hovering a jack here shows nothing. No family is set on the four state inputs or on the Out jack, because they pass whatever is patched into them straight through."
}
