{
  "plugin": "ZetaCarinae",
  "module": "IOU",
  "facts": {
    "inputs": {
      "0": {"family": "cv", "poly": true, "step": "continuous", "negative": "subtracts", "why": "IOU.cpp IOU::process: the jack is added to its knob and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "1": {"family": "cv", "poly": true, "step": "continuous", "negative": "subtracts", "why": "IOU.cpp IOU::process: the jack is added to its knob and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "2": {"family": "cv", "poly": true, "step": "continuous", "negative": "subtracts", "why": "IOU.cpp IOU::process: the jack is added to its knob and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "3": {"family": "cv", "poly": true, "step": "continuous", "negative": "subtracts", "why": "IOU.cpp IOU::process: the jack is added to its knob and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "4": {"poly": true, "step": "continuous", "why": "IOU.cpp IOU::process: the jack is crossfaded into all three outputs by the mix knob and the module counts channels across its input ports and every read is getVoltage(c), so the port is polyphonic, commit 54dbe9b2, the commit that set the version to 2.0.4"}
    },
    "outputs": {
      "0": {"family": "cv", "poly": true, "step": "continuous", "why": "IOU.cpp IOU::process: setChannels is called with the widest input channel count, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "1": {"family": "cv", "poly": true, "step": "continuous", "why": "IOU.cpp IOU::process: setChannels is called with the widest input channel count, commit 54dbe9b2, the commit that set the version to 2.0.4"},
      "2": {"family": "cv", "poly": true, "step": "continuous", "why": "IOU.cpp IOU::process: setChannels is called with the widest input channel count, commit 54dbe9b2, the commit that set the version to 2.0.4"}
    }
  },
  "comment": "None of this module's jacks carries a name in the installed build: the maker added input and output tooltips in the commit after this one, so hovering a jack here shows nothing. No family is set on the external input because it carries whatever is patched into it and is mixed straight through to the outputs."
}
