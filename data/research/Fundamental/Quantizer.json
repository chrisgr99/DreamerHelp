{
  "plugin": "Fundamental",
  "module": "Quantizer",
  "facts": {
    "inputs": {
      "0": {"family": "pitch", "poly": true, "why": "Quantizer.cpp:52 takes getChannels() on this input, and the outputs take their channel count from it"}
    },
    "outputs": {
      "0": {"family": "pitch", "poly": true, "range": "1V per octave", "why": "Quantizer.cpp:66 setChannels after the per-channel write at Quantizer.cpp:64, which writes a semitone count divided by 12"}
    }
  },
  "comment": "55 scale presets ship under presets/Quantizer/ in the installed plugin folder. The keyboard is built from QuantizerButton widgets with no ParamWidget, so the keys cannot be clicked for help; the gesture is described in a note instead."
}
