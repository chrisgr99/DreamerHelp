{
  "plugin": "pachde-hc-one",
  "module": "pachde-hc-favorites",
  "comment": "The module declares one parameter, Current, holding which button is selected. It has no widget of its own — the buttons are drawn by a custom widget — so it can never be clicked and is left untagged."
}
