{
  "plugin": "LilacLoop",
  "module": "LoopMutator",
  "facts": {
    "inputs": {
      "0": {"family": "trigger"},
      "2": {"family": "trigger"},
      "3": {"family": "audio"}
    },
    "outputs": {
      "0": {"family": "audio"}
    }
  },
  "comment": "An overdub button, an overdub input, a second audio input and a second audio output are declared in the code with their widgets commented out, so no click can reach them. The recording buffer is 5,760,000 samples, two minutes at a 48kHz sample rate. Each new recording writes from the start of that buffer, so stepping back to an earlier recording does not restore the audio it held."
}
