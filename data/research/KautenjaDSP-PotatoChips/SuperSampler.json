{
  "plugin": "KautenjaDSP-PotatoChips",
  "module": "SuperSampler",
  "facts": {
    "inputs": {
      "0": {"family": "pitch", "poly": true, "range": "1V per octave", "step": "continuous", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "1": {"family": "pitch", "poly": true, "range": "1V per octave", "step": "continuous", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "2": {"family": "pitch", "poly": true, "range": "1V per octave", "step": "continuous", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "3": {"family": "pitch", "poly": true, "range": "1V per octave", "step": "continuous", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "4": {"family": "pitch", "poly": true, "range": "1V per octave", "step": "continuous", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "5": {"family": "pitch", "poly": true, "range": "1V per octave", "step": "continuous", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "6": {"family": "pitch", "poly": true, "range": "1V per octave", "step": "continuous", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "7": {"family": "pitch", "poly": true, "range": "1V per octave", "step": "continuous", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "8": {"family": "cv", "poly": true, "range": "±5V", "step": "continuous", "negative": "swings", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "9": {"family": "cv", "poly": true, "range": "±5V", "step": "continuous", "negative": "swings", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "10": {"family": "cv", "poly": true, "range": "±5V", "step": "continuous", "negative": "swings", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "11": {"family": "cv", "poly": true, "range": "±5V", "step": "continuous", "negative": "swings", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "12": {"family": "cv", "poly": true, "range": "±5V", "step": "continuous", "negative": "swings", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "13": {"family": "cv", "poly": true, "range": "±5V", "step": "continuous", "negative": "swings", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "14": {"family": "cv", "poly": true, "range": "±5V", "step": "continuous", "negative": "swings", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "15": {"family": "cv", "poly": true, "range": "±5V", "step": "continuous", "negative": "swings", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "17": {"family": "trigger", "poly": true, "range": "high above 2V", "step": "stepped", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "18": {"family": "trigger", "poly": true, "range": "high above 2V", "step": "stepped", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "19": {"family": "trigger", "poly": true, "range": "high above 2V", "step": "stepped", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "20": {"family": "trigger", "poly": true, "range": "high above 2V", "step": "stepped", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "21": {"family": "trigger", "poly": true, "range": "high above 2V", "step": "stepped", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "22": {"family": "trigger", "poly": true, "range": "high above 2V", "step": "stepped", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "23": {"family": "trigger", "poly": true, "range": "high above 2V", "step": "stepped", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "24": {"family": "trigger", "poly": true, "range": "high above 2V", "step": "stepped", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "25": {"family": "trigger", "poly": true, "range": "high above 2V", "step": "stepped", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "26": {"family": "trigger", "poly": true, "range": "high above 2V", "step": "stepped", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "27": {"family": "trigger", "poly": true, "range": "high above 2V", "step": "stepped", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "28": {"family": "trigger", "poly": true, "range": "high above 2V", "step": "stepped", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "29": {"family": "trigger", "poly": true, "range": "high above 2V", "step": "stepped", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "30": {"family": "trigger", "poly": true, "range": "high above 2V", "step": "stepped", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "31": {"family": "trigger", "poly": true, "range": "high above 2V", "step": "stepped", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "32": {"family": "cv", "poly": true, "range": "±10V", "step": "continuous", "negative": "subtracts", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "33": {"family": "cv", "poly": true, "range": "±10V", "step": "continuous", "negative": "subtracts", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "34": {"family": "cv", "poly": true, "range": "±10V", "step": "continuous", "negative": "subtracts", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "35": {"family": "cv", "poly": true, "range": "±10V", "step": "continuous", "negative": "subtracts", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "36": {"family": "cv", "poly": true, "range": "±10V", "step": "continuous", "negative": "subtracts", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "37": {"family": "cv", "poly": true, "range": "±10V", "step": "continuous", "negative": "subtracts", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "38": {"family": "cv", "poly": true, "range": "±10V", "step": "continuous", "negative": "subtracts", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "39": {"family": "cv", "poly": true, "range": "±10V", "step": "continuous", "negative": "subtracts", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "40": {"family": "cv", "poly": true, "range": "±10V", "step": "continuous", "negative": "subtracts", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "41": {"family": "cv", "poly": true, "range": "±10V", "step": "continuous", "negative": "subtracts", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "42": {"family": "cv", "poly": true, "range": "±10V", "step": "continuous", "negative": "subtracts", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "43": {"family": "cv", "poly": true, "range": "±10V", "step": "continuous", "negative": "subtracts", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "44": {"family": "cv", "poly": true, "range": "±10V", "step": "continuous", "negative": "subtracts", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "45": {"family": "cv", "poly": true, "range": "±10V", "step": "continuous", "negative": "subtracts", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "46": {"family": "cv", "poly": true, "range": "±10V", "step": "continuous", "negative": "subtracts", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "47": {"family": "cv", "poly": true, "range": "±10V", "step": "continuous", "negative": "subtracts", "why": "SuperSampler.cpp SuperSampler::processChannel, which reads each jack per channel, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"}
    },
    "outputs": {
      "0": {"family": "audio", "poly": true, "range": "±5V", "step": "continuous", "why": "SuperSampler.cpp processChannel writes 5 times the sample over the 16-bit maximum, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "1": {"family": "audio", "poly": true, "range": "±5V", "step": "continuous", "why": "SuperSampler.cpp processChannel writes 5 times the sample over the 16-bit maximum, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "2": {"family": "audio", "poly": true, "range": "±5V", "step": "continuous", "why": "SuperSampler.cpp processChannel writes 5 times the sample over the 16-bit maximum, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "3": {"family": "audio", "poly": true, "range": "±5V", "step": "continuous", "why": "SuperSampler.cpp processChannel writes 5 times the sample over the 16-bit maximum, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "4": {"family": "audio", "poly": true, "range": "±5V", "step": "continuous", "why": "SuperSampler.cpp processChannel writes 5 times the sample over the 16-bit maximum, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "5": {"family": "audio", "poly": true, "range": "±5V", "step": "continuous", "why": "SuperSampler.cpp processChannel writes 5 times the sample over the 16-bit maximum, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "6": {"family": "audio", "poly": true, "range": "±5V", "step": "continuous", "why": "SuperSampler.cpp processChannel writes 5 times the sample over the 16-bit maximum, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "7": {"family": "audio", "poly": true, "range": "±5V", "step": "continuous", "why": "SuperSampler.cpp processChannel writes 5 times the sample over the 16-bit maximum, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "8": {"family": "audio", "poly": true, "range": "±5V", "step": "continuous", "why": "SuperSampler.cpp processChannel writes 5 times the sample over the 16-bit maximum, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "9": {"family": "audio", "poly": true, "range": "±5V", "step": "continuous", "why": "SuperSampler.cpp processChannel writes 5 times the sample over the 16-bit maximum, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "10": {"family": "audio", "poly": true, "range": "±5V", "step": "continuous", "why": "SuperSampler.cpp processChannel writes 5 times the sample over the 16-bit maximum, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "11": {"family": "audio", "poly": true, "range": "±5V", "step": "continuous", "why": "SuperSampler.cpp processChannel writes 5 times the sample over the 16-bit maximum, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "12": {"family": "audio", "poly": true, "range": "±5V", "step": "continuous", "why": "SuperSampler.cpp processChannel writes 5 times the sample over the 16-bit maximum, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "13": {"family": "audio", "poly": true, "range": "±5V", "step": "continuous", "why": "SuperSampler.cpp processChannel writes 5 times the sample over the 16-bit maximum, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "14": {"family": "audio", "poly": true, "range": "±5V", "step": "continuous", "why": "SuperSampler.cpp processChannel writes 5 times the sample over the 16-bit maximum, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"},
      "15": {"family": "audio", "poly": true, "range": "±5V", "step": "continuous", "why": "SuperSampler.cpp processChannel writes 5 times the sample over the 16-bit maximum, commit 8d368a05, the last commit carrying version 2.0.0 and whose res/ matches the installed panels"}
    }
  },
  "comment": "Parameter 8 and input 16 belong to the first voice's phase modulation, which the chip does not define; the widget skips both, so neither can be reached on the panel. No jack or knob on this module carries a name from the maker, and the panel art carries no lettering."
}
