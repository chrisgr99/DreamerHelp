{
  "plugin": "Bastl",
  "module": "Kompas",
  "facts": {
    "inputs": {
      "0": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "ignored", "normal": "0V", "why": "src/Kompas.cpp:141-146 reads the jack on each clock edge, clamps it to 0V and 10V, rescales that onto 0 to 1023 and adds it to the knob; :145 forces every step on once that number passes 1011. In Per-channel reset mode :143 contributes 0 and :126 uses the jack as a reset instead, high at 2V and re-armed below 0.1V"},
      "1": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "ignored", "normal": "0V", "why": "src/Kompas.cpp:141-146 reads the jack on each clock edge, clamps it to 0V and 10V, rescales that onto 0 to 1023 and adds it to the knob; :145 forces every step on once that number passes 1011. In Per-channel reset mode :143 contributes 0 and :126 uses the jack as a reset instead, high at 2V and re-armed below 0.1V"},
      "2": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "ignored", "normal": "0V", "why": "src/Kompas.cpp:141-146 reads the jack on each clock edge, clamps it to 0V and 10V, rescales that onto 0 to 1023 and adds it to the knob; :145 forces every step on once that number passes 1011. In Per-channel reset mode :143 contributes 0 and :126 uses the jack as a reset instead, high at 2V and re-armed below 0.1V"},
      "3": {"family": "trigger", "poly": false, "range": "high above 2V", "step": "stepped", "negative": "ignored", "normal": "0V", "why": "src/Kompas.cpp:117 rescales the voltage from 0.1V and 2V onto 0 and 1 and puts it through a dsp::SchmittTrigger, so the edge is taken at 2V and re-armed below 0.1V; the step counter is then set to 31 so the next clock wraps to 0"},
      "4": {"family": "trigger", "poly": false, "range": "high above 2V", "step": "stepped", "negative": "ignored", "normal": "0V", "why": "src/Kompas.cpp:133 rescales the voltage from 0.1V and 2V onto 0 and 1 and puts it through a dsp::SchmittTrigger; :153 multiplies each output by clockTrigger.isHigh(), so the outputs follow this jack's high time"}
    },
    "outputs": {
      "0": {"family": "trigger", "poly": false, "range": "0 to 10V", "why": "src/Kompas.cpp:153 writes the step value times clockTrigger.isHigh() times 10 with setVoltage on one channel"},
      "1": {"family": "trigger", "poly": false, "range": "0 to 10V", "why": "src/Kompas.cpp:153 writes the step value times clockTrigger.isHigh() times 10 with setVoltage on one channel"},
      "2": {"family": "trigger", "poly": false, "range": "0 to 10V", "why": "src/Kompas.cpp:153 writes the step value times clockTrigger.isHigh() times 10 with setVoltage on one channel"}
    }
  },
  "comment": "Written against commit bbee836, the last of five commits whose plugin.json carries 2.0.0. That commit replaced two separate menu items with a single submenu carrying \"Per-channel CV control\" and \"Per-channel reset\", and it widened the CV clamp from 5V to 10V, so the shipped build reads the full 0V to 10V.\n\nThe knobs run 0 to 1023 and are displayed as 0 to 1 (src/Kompas.cpp:77-79). Two thresholds inside that range matter and neither is printed anywhere: at or below 2 every step of the pattern is cleared, and above 1011 every step is set. The CV is rescaled onto the same 0 to 1023 scale and added, so CV alone can reach both thresholds.\n\nA pattern is redrawn only when the knob-plus-CV total moves by more than 2 from the value that drew the last one (:174, :239, :281). A knob sitting still therefore keeps its pattern, and the panel's FIND NEW ROUTES lettering is the instruction to move a knob.\n\nAltitude keeps a step where the Latitude and Longitude patterns hold the same value (:290), which includes steps where both are off, not only steps where both are on.\n\nThe three trigger lamps, the three pattern-changed lamps, the reset lamp and the clock lamp are lights rather than parameters, so none can be clicked for help.\n\nThe plugin's manualUrl points at Bastl's hardware product page, which describes the hardware module rather than this port; every figure above was read from the source.\n\nNo control is polyphonic: every read is getVoltage with no channel argument, every write is setVoltage with no channel argument, and nothing calls setChannels."
}
