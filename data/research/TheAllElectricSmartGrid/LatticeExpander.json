{
  "plugin": "TheAllElectricSmartGrid",
  "module": "LatticeExpander",
  "comment": "The module declares no parameters, inputs or outputs at all, so nothing on it can be clicked for help except the title band. The note names are drawn in LedDisplayTextField widgets whose text is rewritten from the module every frame, so typing into one does not stick."
}
