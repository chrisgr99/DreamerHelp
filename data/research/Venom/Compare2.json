{
  "plugin": "Venom",
  "module": "Compare2",
  "facts": {
    "inputs": {
      "0": {"family": "cv", "poly": true, "step": "continuous", "why": "Compare2.cpp:168 reads it per channel and adds it, in volts, to the window position"},
      "1": {"family": "cv", "poly": true, "step": "continuous", "why": "Compare2.cpp:170 reads it per channel and adds it, in volts, to the window size"},
      "2": {"family": "cv", "poly": true, "step": "continuous", "why": "Compare2.cpp:171 reads it per channel and adds it, in volts, to the second window size"},
      "3": {"family": "cv", "poly": true, "step": "continuous", "why": "Compare2.cpp:169 reads it per channel and adds it, in volts, to the second window position"},
      "4": {"poly": true, "step": "continuous", "why": "Compare2.cpp:172 reads it per channel as the value compared against the window"},
      "5": {"poly": true, "step": "continuous", "why": "Compare2.cpp:173 reads it per channel as the value compared against the second window"}
    },
    "outputs": {
      "0": {"family": "trigger", "poly": true, "why": "Compare2.cpp:242 and 245-246 a per-channel SIMD write for every output then setChannels(channels); the widest input sets the count at Compare2.cpp:148-150, and the output range option scales and offsets the gate level"},
      "1": {"family": "trigger", "poly": true, "why": "Compare2.cpp:242 and 245-246 a per-channel SIMD write for every output then setChannels(channels); the widest input sets the count at Compare2.cpp:148-150, and the output range option scales and offsets the gate level"},
      "2": {"family": "trigger", "poly": true, "why": "Compare2.cpp:242 and 245-246 a per-channel SIMD write for every output then setChannels(channels); the widest input sets the count at Compare2.cpp:148-150, and the output range option scales and offsets the gate level"},
      "3": {"family": "trigger", "poly": true, "why": "Compare2.cpp:242 and 245-246 a per-channel SIMD write for every output then setChannels(channels); the widest input sets the count at Compare2.cpp:148-150, and the output range option scales and offsets the gate level"},
      "4": {"family": "trigger", "poly": true, "why": "Compare2.cpp:242 and 245-246 a per-channel SIMD write for every output then setChannels(channels); the widest input sets the count at Compare2.cpp:148-150, and the output range option scales and offsets the gate level"},
      "5": {"family": "trigger", "poly": true, "why": "Compare2.cpp:242 and 245-246 a per-channel SIMD write for every output then setChannels(channels); the widest input sets the count at Compare2.cpp:148-150, and the output range option scales and offsets the gate level"},
      "6": {"family": "trigger", "poly": true, "why": "Compare2.cpp:242 and 245-246 a per-channel SIMD write for every output then setChannels(channels); the widest input sets the count at Compare2.cpp:148-150, and the output range option scales and offsets the gate level"},
      "7": {"family": "trigger", "poly": true, "why": "Compare2.cpp:242 and 245-246 a per-channel SIMD write for every output then setChannels(channels); the widest input sets the count at Compare2.cpp:148-150, and the output range option scales and offsets the gate level"},
      "8": {"family": "trigger", "poly": true, "why": "Compare2.cpp:242 and 245-246 a per-channel SIMD write for every output then setChannels(channels); the widest input sets the count at Compare2.cpp:148-150, and the output range option scales and offsets the gate level"},
      "9": {"family": "trigger", "poly": true, "why": "Compare2.cpp:242 and 245-246 a per-channel SIMD write for every output then setChannels(channels); the widest input sets the count at Compare2.cpp:148-150, and the output range option scales and offsets the gate level"},
      "10": {"family": "trigger", "poly": true, "why": "Compare2.cpp:242 and 245-246 a per-channel SIMD write for every output then setChannels(channels); the widest input sets the count at Compare2.cpp:148-150, and the output range option scales and offsets the gate level"},
      "11": {"family": "trigger", "poly": true, "why": "Compare2.cpp:242 and 245-246 a per-channel SIMD write for every output then setChannels(channels); the widest input sets the count at Compare2.cpp:148-150, and the output range option scales and offsets the gate level"},
      "12": {"family": "trigger", "poly": true, "why": "Compare2.cpp:242 and 245-246 a per-channel SIMD write for every output then setChannels(channels); the widest input sets the count at Compare2.cpp:148-150, and the output range option scales and offsets the gate level"},
      "13": {"family": "trigger", "poly": true, "why": "Compare2.cpp:242 and 245-246 a per-channel SIMD write for every output then setChannels(channels); the widest input sets the count at Compare2.cpp:148-150, and the output range option scales and offsets the gate level"},
      "14": {"family": "trigger", "poly": true, "why": "Compare2.cpp:242 and 245-246 a per-channel SIMD write for every output then setChannels(channels); the widest input sets the count at Compare2.cpp:148-150, and the output range option scales and offsets the gate level"},
      "15": {"family": "trigger", "poly": true, "why": "Compare2.cpp:242 and 245-246 a per-channel SIMD write for every output then setChannels(channels); the widest input sets the count at Compare2.cpp:148-150, and the output range option scales and offsets the gate level"},
      "16": {"family": "trigger", "poly": true, "why": "Compare2.cpp:242 and 245-246 a per-channel SIMD write for every output then setChannels(channels); the widest input sets the count at Compare2.cpp:148-150, and the output range option scales and offsets the gate level"},
      "17": {"family": "trigger", "poly": true, "why": "Compare2.cpp:242 and 245-246 a per-channel SIMD write for every output then setChannels(channels); the widest input sets the count at Compare2.cpp:148-150, and the output range option scales and offsets the gate level"},
      "18": {"family": "trigger", "poly": true, "why": "Compare2.cpp:242 and 245-246 a per-channel SIMD write for every output then setChannels(channels); the widest input sets the count at Compare2.cpp:148-150, and the output range option scales and offsets the gate level"},
      "19": {"family": "trigger", "poly": true, "why": "Compare2.cpp:242 and 245-246 a per-channel SIMD write for every output then setChannels(channels); the widest input sets the count at Compare2.cpp:148-150, and the output range option scales and offsets the gate level"},
      "20": {"family": "trigger", "poly": true, "why": "Compare2.cpp:242 and 245-246 a per-channel SIMD write for every output then setChannels(channels); the widest input sets the count at Compare2.cpp:148-150, and the output range option scales and offsets the gate level"},
      "21": {"family": "trigger", "poly": true, "why": "Compare2.cpp:242 and 245-246 a per-channel SIMD write for every output then setChannels(channels); the widest input sets the count at Compare2.cpp:148-150, and the output range option scales and offsets the gate level"},
      "22": {"family": "trigger", "poly": true, "why": "Compare2.cpp:242 and 245-246 a per-channel SIMD write for every output then setChannels(channels); the widest input sets the count at Compare2.cpp:148-150, and the output range option scales and offsets the gate level"},
      "23": {"family": "trigger", "poly": true, "why": "Compare2.cpp:242 and 245-246 a per-channel SIMD write for every output then setChannels(channels); the widest input sets the count at Compare2.cpp:148-150, and the output range option scales and offsets the gate level"}
    }
  }
}
