{
  "plugin": "Venom",
  "module": "SVF",
  "facts": {
    "inputs": {
      "0": {"family": "pitch", "poly": true, "range": "1V per octave", "step": "continuous", "why": "SVF.cpp:354 reads it per channel and adds it straight into the exponent of the cutoff, one octave per volt"},
      "1": {"family": "pitch", "poly": true, "range": "1V per octave", "step": "continuous", "why": "SVF.cpp:397 reads it per channel and adds it, through a depth knob, into the same exponent as V/Oct"},
      "2": {"family": "cv", "poly": true, "range": "0 to 10V", "step": "continuous", "why": "SVF.cpp:271 divides this jack by 10 onto the 0 to 1 resonance"},
      "3": {"family": "cv", "poly": true, "range": "0 to 10V", "step": "continuous", "why": "SVF.cpp:400 clamps the knob plus this CV to a gain of 10"},
      "4": {"family": "pitch", "poly": true, "range": "1V per octave", "step": "continuous", "why": "SVF.cpp:358 reads it per channel and adds it into the cutoff exponent, one octave per volt"},
      "5": {"family": "cv", "poly": true, "step": "continuous", "why": "SVF.cpp:401 reads it per channel and raises two to the knob plus this CV through a depth knob"},
      "6": {"family": "cv", "poly": true, "range": "0 to 10V", "step": "continuous", "why": "SVF.cpp:275 divides this jack by 10 onto the 0 to 1 morph position"},
      "7": {"family": "audio", "poly": true, "step": "continuous", "why": "SVF.cpp:390 reads it per channel as the signal to be filtered"},
      "8": {"family": "audio", "poly": true, "step": "continuous", "why": "SVF.cpp:391 reads it per channel as the signal to be filtered"}
    },
    "outputs": {
      "0": {"family": "audio", "poly": true, "why": "SVF.cpp:576 a per-channel write and 602-611 setChannels(channels); the widest input sets the count at SVF.cpp:341-346"},
      "1": {"family": "audio", "poly": true, "why": "SVF.cpp:577 a per-channel write and 602-611 setChannels(channels); the widest input sets the count at SVF.cpp:341-346"},
      "2": {"family": "audio", "poly": true, "why": "SVF.cpp:581 a per-channel write and 602-611 setChannels(channels); the widest input sets the count at SVF.cpp:341-346"},
      "3": {"family": "audio", "poly": true, "why": "SVF.cpp:582 a per-channel write and 602-611 setChannels(channels); the widest input sets the count at SVF.cpp:341-346"},
      "4": {"family": "audio", "poly": true, "why": "SVF.cpp:586 a per-channel write and 602-611 setChannels(channels); the widest input sets the count at SVF.cpp:341-346"},
      "5": {"family": "audio", "poly": true, "why": "SVF.cpp:587 a per-channel write and 602-611 setChannels(channels); the widest input sets the count at SVF.cpp:341-346"},
      "6": {"family": "audio", "poly": true, "why": "SVF.cpp:591 a per-channel write and 602-611 setChannels(channels); the widest input sets the count at SVF.cpp:341-346"},
      "7": {"family": "audio", "poly": true, "why": "SVF.cpp:592 a per-channel write and 602-611 setChannels(channels); the widest input sets the count at SVF.cpp:341-346"},
      "8": {"family": "audio", "poly": true, "why": "SVF.cpp:596 a per-channel write and 602-611 setChannels(channels); the widest input sets the count at SVF.cpp:341-346"},
      "9": {"family": "audio", "poly": true, "why": "SVF.cpp:597 a per-channel write and 602-611 setChannels(channels); the widest input sets the count at SVF.cpp:341-346"}
    }
  }
}
