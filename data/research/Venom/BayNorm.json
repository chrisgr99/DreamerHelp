{
  "plugin": "Venom",
  "module": "BayNorm",
  "facts": {
    "inputs": {
      "0": {"poly": true, "why": "BayNorm.cpp:31-34 copies every channel of this jack to the output of the same number when the linked Bay Input's port 1 is unpatched; :44-47 the same when no Bay Input is linked or it is bypassed"},
      "1": {"poly": true, "why": "BayNorm.cpp:31-34 copies every channel of this jack to the output of the same number when the linked Bay Input's port 2 is unpatched; :44-47 the same when no Bay Input is linked or it is bypassed"},
      "2": {"poly": true, "why": "BayNorm.cpp:31-34 copies every channel of this jack to the output of the same number when the linked Bay Input's port 3 is unpatched; :44-47 the same when no Bay Input is linked or it is bypassed"},
      "3": {"poly": true, "why": "BayNorm.cpp:31-34 copies every channel of this jack to the output of the same number when the linked Bay Input's port 4 is unpatched; :44-47 the same when no Bay Input is linked or it is bypassed"},
      "4": {"poly": true, "why": "BayNorm.cpp:31-34 copies every channel of this jack to the output of the same number when the linked Bay Input's port 5 is unpatched; :44-47 the same when no Bay Input is linked or it is bypassed"},
      "5": {"poly": true, "why": "BayNorm.cpp:31-34 copies every channel of this jack to the output of the same number when the linked Bay Input's port 6 is unpatched; :44-47 the same when no Bay Input is linked or it is bypassed"},
      "6": {"poly": true, "why": "BayNorm.cpp:31-34 copies every channel of this jack to the output of the same number when the linked Bay Input's port 7 is unpatched; :44-47 the same when no Bay Input is linked or it is bypassed"},
      "7": {"poly": true, "why": "BayNorm.cpp:31-34 copies every channel of this jack to the output of the same number when the linked Bay Input's port 8 is unpatched; :44-47 the same when no Bay Input is linked or it is bypassed"}
    },
    "outputs": {
      "0": {"poly": true, "why": "BayNorm.cpp:26-39 and 45-51 setChannels(that input's channel count) then a per-channel write; with a BAY INPUT selected the width comes from that module's matching input instead"},
      "1": {"poly": true, "why": "BayNorm.cpp:26-39 and 45-51 setChannels(that input's channel count) then a per-channel write; with a BAY INPUT selected the width comes from that module's matching input instead"},
      "2": {"poly": true, "why": "BayNorm.cpp:26-39 and 45-51 setChannels(that input's channel count) then a per-channel write; with a BAY INPUT selected the width comes from that module's matching input instead"},
      "3": {"poly": true, "why": "BayNorm.cpp:26-39 and 45-51 setChannels(that input's channel count) then a per-channel write; with a BAY INPUT selected the width comes from that module's matching input instead"},
      "4": {"poly": true, "why": "BayNorm.cpp:26-39 and 45-51 setChannels(that input's channel count) then a per-channel write; with a BAY INPUT selected the width comes from that module's matching input instead"},
      "5": {"poly": true, "why": "BayNorm.cpp:26-39 and 45-51 setChannels(that input's channel count) then a per-channel write; with a BAY INPUT selected the width comes from that module's matching input instead"},
      "6": {"poly": true, "why": "BayNorm.cpp:26-39 and 45-51 setChannels(that input's channel count) then a per-channel write; with a BAY INPUT selected the width comes from that module's matching input instead"},
      "7": {"poly": true, "why": "BayNorm.cpp:26-39 and 45-51 setChannels(that input's channel count) then a per-channel write; with a BAY INPUT selected the width comes from that module's matching input instead"}
    }
  }
}
