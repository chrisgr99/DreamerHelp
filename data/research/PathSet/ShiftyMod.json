{
  "plugin": "PathSet",
  "module": "ShiftyMod",
  "facts": {
    "inputs": {
      "0": {"family": "trigger", "poly": false, "step": "stepped", "range": "high above 2V", "normal": "the module's own internal clock", "why": "ShiftyMod.cpp:210 tests this input against 2V with getVoltage() alone, and ShiftyMod.cpp:203 runs the internal clock at the CLOCK RATE knob instead when nothing is patched"},
      "1": {"family": "trigger", "poly": false, "step": "stepped", "range": "high above 2V", "why": "ShiftyMod.cpp:229 tests this input against 2V with getVoltage() alone; ShiftyMod.cpp:222 takes the hit from the clock divider instead when nothing is patched here"},
      "2": {"family": "cv", "poly": false, "step": "stepped", "range": "0 to 10V", "normal": "a fresh random value on every hit", "why": "ShiftyMod.cpp:262 divides this input by 10 onto the 0 to 1 delay, ShiftyMod.cpp:264 falls back to the row's own noise value when nothing is patched, and ShiftyMod.cpp:275 floors the result into a hit-queue index"},
      "3": {"family": "cv", "poly": false, "step": "stepped", "range": "0 to 10V", "normal": "a fresh random value on every hit", "why": "ShiftyMod.cpp:262 divides this input by 10 onto the 0 to 1 delay, ShiftyMod.cpp:264 falls back to the row's own noise value when nothing is patched, and ShiftyMod.cpp:275 floors the result into a hit-queue index"},
      "4": {"family": "cv", "poly": false, "step": "stepped", "range": "0 to 10V", "normal": "a fresh random value on every hit", "why": "ShiftyMod.cpp:262 divides this input by 10 onto the 0 to 1 delay, ShiftyMod.cpp:264 falls back to the row's own noise value when nothing is patched, and ShiftyMod.cpp:275 floors the result into a hit-queue index"},
      "5": {"family": "cv", "poly": false, "step": "stepped", "range": "0 to 10V", "normal": "a fresh random value on every hit", "why": "ShiftyMod.cpp:262 divides this input by 10 onto the 0 to 1 delay, ShiftyMod.cpp:264 falls back to the row's own noise value when nothing is patched, and ShiftyMod.cpp:275 floors the result into a hit-queue index"},
      "6": {"family": "cv", "poly": false, "step": "stepped", "range": "0 to 10V", "normal": "a fresh random value on every hit", "why": "ShiftyMod.cpp:262 divides this input by 10 onto the 0 to 1 delay, ShiftyMod.cpp:264 falls back to the row's own noise value when nothing is patched, and ShiftyMod.cpp:275 floors the result into a hit-queue index"},
      "7": {"family": "cv", "poly": false, "step": "stepped", "range": "0 to 10V", "normal": "a fresh random value on every hit", "why": "ShiftyMod.cpp:262 divides this input by 10 onto the 0 to 1 delay, ShiftyMod.cpp:264 falls back to the row's own noise value when nothing is patched, and ShiftyMod.cpp:275 floors the result into a hit-queue index"},
      "8": {"family": "cv", "poly": false, "step": "stepped", "range": "0 to 10V", "normal": "a fresh random value on every hit", "why": "ShiftyMod.cpp:262 divides this input by 10 onto the 0 to 1 delay, ShiftyMod.cpp:264 falls back to the row's own noise value when nothing is patched, and ShiftyMod.cpp:275 floors the result into a hit-queue index"}
    },
    "outputs": {
      "0": {"family": "trigger"},
      "1": {"family": "trigger"},
      "2": {"family": "trigger"},
      "3": {"family": "trigger"},
      "4": {"family": "trigger"},
      "5": {"family": "trigger"},
      "6": {"family": "trigger"}
    }
  },
  "comment": "The clock divider parameter runs 0.6 to 15.6 and is floored, so the printed range on the tooltip is one step wider than the whole numbers it produces."
}
