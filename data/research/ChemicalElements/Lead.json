{
  "plugin": "ChemicalElements",
  "module": "Lead",
  "facts": {
    "outputs": {
      "0": {"family": "cv", "poly": false, "range": "±10V", "step": "continuous", "why": "src/Lead.cpp:31 calls setVoltage with the parameter value on channel 0 only; the parameter is configured -10 to 10 at src/Lead.cpp:24"},
      "1": {"family": "cv", "poly": false, "range": "±10V", "step": "continuous", "why": "src/Lead.cpp:32 calls setVoltage with the parameter value on channel 0 only; the parameter is configured -10 to 10 at src/Lead.cpp:25"}
    }
  },
  "comment": "Read from the repository at commit 414e488, the tip of the default branch; every commit in the repository carries version 2.0.0 in plugin.json, so the installed build cannot be narrowed further than that.\n\nThe repository holds a Carbon.svg panel in res/ and assets/, but no Carbon source file and no Carbon model in plugin.json, so there is no third module."
}
