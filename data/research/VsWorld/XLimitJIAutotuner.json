{
  "plugin": "VsWorld",
  "module": "XLimitJIAutotuner",
  "facts": {
    "inputs": {
      "0": {"family": "pitch", "poly": true, "range": "1V per octave", "step": "continuous", "negative": "swings", "why": "src/XLimitJIAutotuner.cpp:254 getChannels() sets the loop bound; :279 getPolyVoltage(c)"}
    },
    "outputs": {
      "0": {"family": "pitch", "poly": true, "range": "1V per octave", "why": "src/XLimitJIAutotuner.cpp:256 setChannels(channels); :282 clampD(baseVoltage+harmonicVoltage,-10,10)"},
      "1": {"family": "pitch", "poly": true, "why": "src/XLimitJIAutotuner.cpp:284 voutR = vout - (double)voutF, the double-to-float remainder; :257 setChannels"}
    }
  },
  "comment": "REMAP_PARAM is parameter 8 and has no configParam and no widget, so it cannot be clicked and carries no tooltip; the only way at it is the context menu item, which reads and writes the parameter directly. Panel order and index order agree for the eight knobs. The tuning circle is a plain Widget with no parameter, so it cannot be clicked for help either. The README describes dynamic tuning as the default and static tuning as feeding the base key into the first channel; the code has no setting for this, it simply always measures every channel against channel 0."
}
