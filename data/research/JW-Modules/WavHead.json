{
  "plugin": "JW-Modules",
  "module": "WavHead",
  "facts": {
    "inputs": {
      "0": {"family": "cv", "poly": true, "step": "continuous", "why": "JW-Modules 2.0.43 src/WavHead.cpp:68: getChannels() on this port sets how many heads are drawn and every channel is read with getVoltage(c); read per channel over getChannels()"}
    }
  }
}
