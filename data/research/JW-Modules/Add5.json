{
  "plugin": "JW-Modules",
  "module": "Add5",
  "facts": {
    "inputs": {
      "0": {"poly": true, "step": "continuous", "why": "JW-Modules 2.0.43 src/Add5.cpp:52: getChannels() on this port sets the width of the output beside it and every channel is read with getVoltage(c); read per channel over getChannels()"},
      "1": {"poly": true, "step": "continuous", "why": "JW-Modules 2.0.43 src/Add5.cpp:52: getChannels() on this port sets the width of the output beside it and every channel is read with getVoltage(c); read per channel over getChannels()"},
      "2": {"poly": true, "step": "continuous", "why": "JW-Modules 2.0.43 src/Add5.cpp:52: getChannels() on this port sets the width of the output beside it and every channel is read with getVoltage(c); read per channel over getChannels()"},
      "3": {"poly": true, "step": "continuous", "why": "JW-Modules 2.0.43 src/Add5.cpp:52: getChannels() on this port sets the width of the output beside it and every channel is read with getVoltage(c); read per channel over getChannels()"},
      "4": {"poly": true, "step": "continuous", "why": "JW-Modules 2.0.43 src/Add5.cpp:52: getChannels() on this port sets the width of the output beside it and every channel is read with getVoltage(c); read per channel over getChannels()"},
      "5": {"poly": true, "step": "continuous", "why": "JW-Modules 2.0.43 src/Add5.cpp:52: getChannels() on this port sets the width of the output beside it and every channel is read with getVoltage(c); read per channel over getChannels()"},
      "6": {"poly": true, "step": "continuous", "why": "JW-Modules 2.0.43 src/Add5.cpp:52: getChannels() on this port sets the width of the output beside it and every channel is read with getVoltage(c); read per channel over getChannels()"},
      "7": {"poly": true, "step": "continuous", "why": "JW-Modules 2.0.43 src/Add5.cpp:52: getChannels() on this port sets the width of the output beside it and every channel is read with getVoltage(c); read per channel over getChannels()"},
      "8": {"poly": true, "step": "continuous", "why": "JW-Modules 2.0.43 src/Add5.cpp:52: getChannels() on this port sets the width of the output beside it and every channel is read with getVoltage(c); read per channel over getChannels()"},
      "9": {"poly": true, "step": "continuous", "why": "JW-Modules 2.0.43 src/Add5.cpp:52: getChannels() on this port sets the width of the output beside it and every channel is read with getVoltage(c); read per channel over getChannels()"},
      "10": {"poly": true, "step": "continuous", "why": "JW-Modules 2.0.43 src/Add5.cpp:52: getChannels() on this port sets the width of the output beside it and every channel is read with getVoltage(c); read per channel over getChannels()"},
      "11": {"poly": true, "step": "continuous", "why": "JW-Modules 2.0.43 src/Add5.cpp:52: getChannels() on this port sets the width of the output beside it and every channel is read with getVoltage(c); read per channel over getChannels()"},
      "12": {"poly": true, "step": "continuous", "why": "JW-Modules 2.0.43 src/Add5.cpp:52: getChannels() on this port sets the width of the output beside it and every channel is read with getVoltage(c); read per channel over getChannels()"},
      "13": {"poly": true, "step": "continuous", "why": "JW-Modules 2.0.43 src/Add5.cpp:52: getChannels() on this port sets the width of the output beside it and every channel is read with getVoltage(c); read per channel over getChannels()"},
      "14": {"poly": true, "step": "continuous", "why": "JW-Modules 2.0.43 src/Add5.cpp:52: getChannels() on this port sets the width of the output beside it and every channel is read with getVoltage(c); read per channel over getChannels()"},
      "15": {"poly": true, "step": "continuous", "why": "JW-Modules 2.0.43 src/Add5.cpp:52: getChannels() on this port sets the width of the output beside it and every channel is read with getVoltage(c); read per channel over getChannels()"}
    }
  }
}
