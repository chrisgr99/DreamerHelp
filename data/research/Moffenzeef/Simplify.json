{
  "plugin": "Moffenzeef",
  "module": "Simplify",
  "facts": {
    "inputs": {
      "0": {"family": "trigger", "poly": false, "range": "high above 0.5V", "step": "stepped", "why": "Moffenzeef 2.6.1 source (github.com/moffenzeefmodular/VCVrack-submission commit e6a0c67), src/Simplify.cpp:63: bangState = inputs[BANG_INPUT].getVoltage() > 0.5f; mono: the plugin reads every input with getVoltage() and the only setChannels in src is Kleztizer.cpp:305"},
      "1": {"family": "trigger", "poly": false, "range": "high above 0.5V", "step": "stepped", "why": "Moffenzeef 2.6.1 source (github.com/moffenzeefmodular/VCVrack-submission commit e6a0c67), src/Simplify.cpp:64: resetState = inputs[RESET_INPUT].getVoltage() > 0.5f; mono: the plugin reads every input with getVoltage() and the only setChannels in src is Kleztizer.cpp:305"},
      "2": {"family": "cv", "poly": false, "range": "±5V", "negative": "subtracts", "why": "Moffenzeef 2.6.1 source (github.com/moffenzeefmodular/VCVrack-submission commit e6a0c67), src/Simplify.cpp:52-57: normalizedCV = (cvInput + 5.0f) / 10.0f then knob1Value = knob1Param + (normalizedCV - 0.5f), clamped 0..1; mono: the plugin reads every input with getVoltage() and the only setChannels in src is Kleztizer.cpp:305", "docRange": "±5V"}
    },
    "outputs": {
      "0": {"family": "trigger"}
    }
  },
  "comment": "Read from the maker's source at 2.6.1, commit e6a0c67. The pattern index is clamp(knob/137 + cv*0.1, 0, 1)*136, and the patterns are a table of 68-byte rows, each row holding its own length. The knob is configured with a range of 1 to 137."
}
