{
  "plugin": "VostokInstruments",
  "module": "Trace",
  "facts": {
    "inputs": {
      "0": {"family": "cv", "poly": false, "step": "continuous", "negative": "subtracts", "range": "±5V", "sumRange": "0 to 1V", "why": "src/Trace.cpp:55-57 reads this input with getVoltage, divides by 5, scales by the SCAN CV knob, applies the +/- switch and clamps the sum with the slider to 0-1"},
      "1": {"poly": false, "step": "continuous", "negative": "swings", "why": "src/Trace.cpp:69-70 reads each of the four inputs with getVoltage and multiplies by a gain from gainsForChannels"},
      "2": {"poly": false, "step": "continuous", "negative": "swings", "why": "src/Trace.cpp:69-70 reads each of the four inputs with getVoltage and multiplies by a gain from gainsForChannels"},
      "3": {"poly": false, "step": "continuous", "negative": "swings", "why": "src/Trace.cpp:69-70 reads each of the four inputs with getVoltage and multiplies by a gain from gainsForChannels"},
      "4": {"poly": false, "step": "continuous", "negative": "swings", "why": "src/Trace.cpp:69-70 reads each of the four inputs with getVoltage and multiplies by a gain from gainsForChannels"}
    },
    "outputs": {
      "0": {"poly": false, "range": "±10V", "why": "src/Trace.cpp:72-76 clamps the sum to ±10V when the Clip Output menu item is on and writes it with setVoltage"}
    }
  },
  "comment": "The four signal inputs and the output carry whatever you patch in, so they are given no family. sumRange on the scan input is written as a voltage range because the field takes only those four spellings; the quantity clamped is the 0-to-1 scan position. The maker's second line on the scan input reads 'Expects 5V peak-to-peak, combines with SCAN slider', which the code bears out. The crossfade law is the same cubic Path uses."
}
