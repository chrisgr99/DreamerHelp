{
  "plugin": "FrequencyDomain",
  "module": "BoxOfRevelation",
  "facts": {
    "inputs": {
      "0": {"family": "audio", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/BoxOfRevelation.cpp:596 reads this jack as getVoltage()/5.0, so 5V is full scale into the filter chain, and line 639 multiplies the result back by 5 on the way out. getVoltage() reads channel 1 only."},
      "1": {"family": "audio", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "normal": "0V", "why": "src/controller/BoxOfRevelation.cpp:597 reads this jack as getVoltage()/5.0 with no isConnected test and nothing normalled to it, so an unpatched jack feeds 0V into the second filter chain. getVoltage() reads channel 1 only."},
      "2": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "stepped", "negative": "subtracts", "why": "src/controller/BoxOfRevelation.cpp:338 hands this jack to paramValue(), defined at src/controller/BoxOfRevelation.cpp:99: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0 to the number of cubes in the loaded file minus one, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels. The result is stored in an int, so it steps cube by cube."},
      "3": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/BoxOfRevelation.cpp:368 hands this jack to paramValue(), defined at src/controller/BoxOfRevelation.cpp:99: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0 to 1, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "4": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/BoxOfRevelation.cpp:373 hands this jack to paramValue(), defined at src/controller/BoxOfRevelation.cpp:99: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0 to 1, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "5": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/BoxOfRevelation.cpp:375 hands this jack to paramValue(), defined at src/controller/BoxOfRevelation.cpp:99: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0 to 1, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "6": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/BoxOfRevelation.cpp:387 hands this jack to paramValue(), defined at src/controller/BoxOfRevelation.cpp:99: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0 to 1, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels. Line 378 skips this call while LINK is on, so the jack does nothing in that mode."},
      "7": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/BoxOfRevelation.cpp:392 hands this jack to paramValue(), defined at src/controller/BoxOfRevelation.cpp:99: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0 to 1, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels. Line 378 skips this call while LINK is on, so the jack does nothing in that mode."},
      "8": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/BoxOfRevelation.cpp:394 hands this jack to paramValue(), defined at src/controller/BoxOfRevelation.cpp:99: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0 to 1, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels. Line 378 skips this call while LINK is on, so the jack does nothing in that mode."}
    },
    "outputs": {
      "0": {"family": "audio", "poly": false, "range": "±10V", "why": "src/controller/BoxOfRevelation.cpp:639 writes clamp(out * 5, -10, 10), so the jack is held inside ±10V. setChannels() is never called, so it stays monophonic."},
      "1": {"family": "audio", "poly": false, "range": "±10V", "why": "src/controller/BoxOfRevelation.cpp:640 writes clamp(out * 5, -10, 10), so the jack is held inside ±10V. setChannels() is never called, so it stays monophonic."}
    }
  }
}
