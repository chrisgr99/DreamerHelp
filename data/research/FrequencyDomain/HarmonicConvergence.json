{
  "plugin": "FrequencyDomain",
  "module": "HarmonicConvergence",
  "facts": {
    "inputs": {
      "0": {"family": "audio", "poly": false, "why": "src/controller/HarmonicConvergence.cpp:275 reads this jack with a bare getVoltage(); line 315 adds the feedback and multiplies by the window value before the FFT, with no scaling or clamp. Line 272 tests isConnected() to decide whether this side is morphed with the other."},
      "1": {"family": "audio", "poly": false, "normal": "0V", "why": "src/controller/HarmonicConvergence.cpp:276 reads this jack with a bare getVoltage() and line 316 feeds it to the second FFT. Nothing is normalled to it; line 273 tests isConnected() to decide whether this side is morphed with the other."},
      "2": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/HarmonicConvergence.cpp:464 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftX, and src/model/Cells.hpp:334 adds that figure times the grid’s full value range to every band. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "3": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/HarmonicConvergence.cpp:465 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftY, and src/model/Cells.hpp:309 adds that figure times the grid’s height to every band position. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "4": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/HarmonicConvergence.cpp:469 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftX, and src/model/Cells.hpp:334 adds that figure times the grid’s full value range to every band. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "5": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/HarmonicConvergence.cpp:470 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftY, and src/model/Cells.hpp:309 adds that figure times the grid’s height to every band position. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "6": {"family": "audio", "poly": true, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/HarmonicConvergence.cpp:545 loops to this jack’s getChannels() and reads getVoltage(i) per channel; the README at line 154 says the frequency modulation inputs are polyphonic and that the two jacks together carry up to 32. Line 547 multiplies each channel by 2000, the maker’s comment giving ±5V as ±10000Hz."},
      "7": {"family": "audio", "poly": true, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/HarmonicConvergence.cpp:550 loops to this jack’s getChannels() and reads getVoltage(i) per channel; the README at line 154 says the frequency modulation inputs are polyphonic and that the two jacks together carry up to 32. Line 552 multiplies each channel by 2000, the maker’s comment giving ±5V as ±10000Hz. Its channels are counted on from the first FM jack’s."},
      "8": {"family": "audio", "poly": true, "range": "0 to 5V", "step": "continuous", "negative": "ignored", "why": "src/controller/HarmonicConvergence.cpp:558 loops to this jack’s getChannels() and reads getVoltage(i) per channel; the README at line 157 says the RM/AM inputs are polyphonic and that the two jacks together carry up to 32. src/model/OscillatorBank.hpp:66 divides each channel by 5, and line 180 drops anything below 0 before multiplying, so a negative voltage silences rather than inverts."},
      "9": {"family": "audio", "poly": true, "range": "0 to 5V", "step": "continuous", "negative": "ignored", "why": "src/controller/HarmonicConvergence.cpp:563 loops to this jack’s getChannels() and reads getVoltage(i) per channel; the README at line 157 says the RM/AM inputs are polyphonic and that the two jacks together carry up to 32. src/model/OscillatorBank.hpp:66 divides each channel by 5, and line 180 drops anything below 0 before multiplying. Its channels are counted on from the first AM/RM jack’s."},
      "10": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "stepped", "negative": "subtracts", "why": "src/controller/HarmonicConvergence.cpp:450 hands this jack to paramValue(), defined at src/controller/HarmonicConvergence.cpp:253: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 1 to 36, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels. The result is cast to a uint8_t, so it steps voice by voice."},
      "11": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "stepped", "negative": "subtracts", "why": "src/controller/HarmonicConvergence.cpp:575 hands this jack to paramValue(), defined at src/controller/HarmonicConvergence.cpp:253: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0 to 4, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels. The result is cast to a uint8_t, so it steps through the five waveforms."},
      "14": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/HarmonicConvergence.cpp:455 hands this jack to paramValue(), defined at src/controller/HarmonicConvergence.cpp:253: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans -3 to 3, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "15": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/HarmonicConvergence.cpp:493 hands this jack to paramValue(), defined at src/controller/HarmonicConvergence.cpp:253: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0 to 1, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "16": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/HarmonicConvergence.cpp:494 hands this jack to paramValue(), defined at src/controller/HarmonicConvergence.cpp:253: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0 to 1, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "17": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/HarmonicConvergence.cpp:581 hands this jack to paramValue(), defined at src/controller/HarmonicConvergence.cpp:253: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0 to 100, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "18": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/HarmonicConvergence.cpp:475 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftX, and src/model/Cells.hpp:334 adds that figure times the grid’s full value range to every band. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "19": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/HarmonicConvergence.cpp:476 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftY, and src/model/Cells.hpp:309 adds that figure times the grid’s height to every band position. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "20": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/HarmonicConvergence.cpp:480 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftX, and src/model/Cells.hpp:334 adds that figure times the grid’s full value range to every band. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "21": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/HarmonicConvergence.cpp:481 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftY, and src/model/Cells.hpp:309 adds that figure times the grid’s height to every band position. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "22": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "stepped", "negative": "subtracts", "why": "src/controller/HarmonicConvergence.cpp:571 hands this jack to paramValue(), defined at src/controller/HarmonicConvergence.cpp:253: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans -35 to 35, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels. The result is cast to an int8_t, so it steps voice by voice."},
      "23": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/HarmonicConvergence.cpp:458 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftX, and src/model/Cells.hpp:334 adds that figure times the grid’s full value range to every band. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "24": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/HarmonicConvergence.cpp:459 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftY, and src/model/Cells.hpp:309 adds that figure times the grid’s height to every band position. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "25": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/HarmonicConvergence.cpp:487 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftX, and src/model/Cells.hpp:334 adds that figure times the grid’s full value range to every band. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "26": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/HarmonicConvergence.cpp:488 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftY, and src/model/Cells.hpp:309 adds that figure times the grid’s height to every band position. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "27": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "stepped", "negative": "subtracts", "why": "src/controller/HarmonicConvergence.cpp:389 hands this jack to paramValue(), defined at src/controller/HarmonicConvergence.cpp:253: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0 to 3, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels. The result is cast to a uint8_t sort mode, so it steps through the four modes. It is only read when a frame has just been analysed."},
      "28": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/HarmonicConvergence.cpp:497 hands this jack to paramValue(), defined at src/controller/HarmonicConvergence.cpp:253: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0 to 1, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "29": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/HarmonicConvergence.cpp:500 hands this jack to paramValue(), defined at src/controller/HarmonicConvergence.cpp:253: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0 to 1, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "30": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/HarmonicConvergence.cpp:269 hands this jack to paramValue(), defined at src/controller/HarmonicConvergence.cpp:253: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0 to 1, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels."}
    },
    "outputs": {
      "0": {"family": "audio", "poly": false, "why": "src/controller/HarmonicConvergence.cpp:591 and 595 write the oscillator bank sum interpolated against the delayed dry signal by MIX, with no clamp, so no range can be stated. setChannels() is never called."},
      "1": {"family": "audio", "poly": false, "why": "src/controller/HarmonicConvergence.cpp:588 tests this jack for isConnected(): with nothing patched here the module sums to mono on the left jack alone, and only when a cable is in does line 596 write the right side. There is no clamp."}
    }
  },
  "comment": "The FEEDBACK knob is drawn on the panel but never named in the code, so Rack shows it with no tooltip; it scales the output fed back into both analysis inputs. The FREQ. SHIFT knob is named in octaves and documented as ±3 octaves, but the code multiplies the analysed frequency by 1 plus the setting going up and by 1 plus a third of it going down."
}
