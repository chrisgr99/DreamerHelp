{
  "plugin": "FrequencyDomain",
  "module": "Morphology",
  "facts": {
    "inputs": {
      "0": {"family": "audio", "poly": false, "why": "src/controller/Morphology.cpp:210 reads this jack with a bare getVoltage() and line 397 multiplies it by the window value before the FFT, with no scaling or clamp, so no range can be stated. getVoltage() reads channel 1 only."},
      "1": {"family": "audio", "poly": false, "normal": "0V", "why": "src/controller/Morphology.cpp:211 reads this jack with a bare getVoltage() and line 398 multiplies it by the window value before the second FFT. Nothing is normalled to it and there is no scaling or clamp. getVoltage() reads channel 1 only."},
      "2": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/Morphology.cpp:381 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftX, and src/model/Cells.hpp:334 adds that figure times the grid’s full value range to every band. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "3": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/Morphology.cpp:382 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftY, and src/model/Cells.hpp:309 adds that figure times the grid’s height to every band position. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "4": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/Morphology.cpp:386 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftX, and src/model/Cells.hpp:334 adds that figure times the grid’s full value range to every band. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "5": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/Morphology.cpp:387 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftY, and src/model/Cells.hpp:309 adds that figure times the grid’s height to every band position. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "6": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/Morphology.cpp:377 hands this jack to paramValue(), defined at src/controller/Morphology.cpp:197: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans minus the band count to plus the band count, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels. The band count follows the frame size, so the span this jack covers changes with it."},
      "7": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/Morphology.cpp:294 hands this jack to paramValue(), defined at src/controller/Morphology.cpp:197: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0 to 1, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "8": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/Morphology.cpp:332 hands this jack to paramValue(), defined at src/controller/Morphology.cpp:197: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0 to 1, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "9": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/Morphology.cpp:296 hands this jack to paramValue(), defined at src/controller/Morphology.cpp:197: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans -1 to 1, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "10": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/Morphology.cpp:334 hands this jack to paramValue(), defined at src/controller/Morphology.cpp:197: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans -1 to 1, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "11": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/Morphology.cpp:371 hands this jack to paramValue(), defined at src/controller/Morphology.cpp:197: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0 to 50, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "12": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/Morphology.cpp:373 hands this jack to paramValue(), defined at src/controller/Morphology.cpp:197: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0 to 50, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels."}
    },
    "outputs": {
      "0": {"family": "audio", "poly": false, "why": "src/controller/Morphology.cpp:491 writes the summed inverse FFT frames with no clamp and no scaling, so no range can be stated. setChannels() is never called."},
      "1": {"family": "audio", "poly": false, "why": "src/controller/Morphology.cpp:492 writes the summed inverse FFT frames with no clamp and no scaling, so no range can be stated. setChannels() is never called."}
    }
  },
  "comment": "The frame size parameter and a third output are declared with no widget on the panel, so they can never be clicked and are left untagged."
}
