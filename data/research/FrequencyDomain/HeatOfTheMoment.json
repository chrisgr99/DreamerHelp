{
  "plugin": "FrequencyDomain",
  "module": "HeatOfTheMoment",
  "facts": {
    "inputs": {
      "0": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "stepped", "negative": "subtracts", "why": "src/controller/HeatOfTheMoment.cpp:78 hands this jack to paramValue(), defined at src/controller/HeatOfTheMoment.cpp:63: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0 to 6, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels. The result is cast to a NoiseColor, so it steps through the seven colours."},
      "1": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "stepped", "negative": "subtracts", "why": "src/controller/HeatOfTheMoment.cpp:80 hands this jack to paramValue(), defined at src/controller/HeatOfTheMoment.cpp:63: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0 to 7, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels. The result is stored in an int, so it steps through the eight window functions."},
      "2": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/HeatOfTheMoment.cpp:87 hands this jack to paramValue(), defined at src/controller/HeatOfTheMoment.cpp:63: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0.1 to 100, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "3": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "stepped", "negative": "subtracts", "why": "src/controller/HeatOfTheMoment.cpp:85 hands this jack to paramValue(), defined at src/controller/HeatOfTheMoment.cpp:63: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 1 to 4, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels. The result is stored in an int, so it steps 1, 2, 3, 4."},
      "4": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/HeatOfTheMoment.cpp:89 hands this jack to paramValue(), defined at src/controller/HeatOfTheMoment.cpp:63: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 1 to 1000, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "5": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "negative": "ignored", "why": "src/controller/HeatOfTheMoment.cpp:96 passes the jack to a dsp::SchmittTrigger, whose Rack defaults fire above 1V and reset below 0V; line 101 then tests the same voltage for greater than 0 to keep repeating while the gate is held. getVoltage() reads channel 1 only."}
    },
    "outputs": {
      "0": {"family": "audio", "poly": false, "why": "src/controller/HeatOfTheMoment.cpp:122 sets one voltage with setVoltage() and never calls setChannels(), so the jack stays monophonic. The value is raw noise times two window values and is not clamped, so no range can be stated."}
    }
  }
}
