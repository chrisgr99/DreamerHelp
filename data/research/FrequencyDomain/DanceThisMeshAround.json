{
  "plugin": "FrequencyDomain",
  "module": "DanceThisMeshAround",
  "facts": {
    "inputs": {
      "0": {"family": "audio", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/DanceThisMeshAround.cpp:214 reads this jack as getVoltage()/5.0 before the group feedback is added, so 5V is full scale into the mesh; line 215 then hard limits the sum to ±1. getVoltage() reads channel 1 only."},
      "1": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "stepped", "negative": "subtracts", "why": "src/controller/DanceThisMeshAround.cpp:125 hands this jack to paramValue(), defined at src/controller/DanceThisMeshAround.cpp:108: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 2 to 5, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels. The result is stored in an int, so it steps 2, 3, 4, 5."},
      "2": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/DanceThisMeshAround.cpp:207 hands this jack to paramValue(), defined at src/controller/DanceThisMeshAround.cpp:108: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 1 to 5, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "3": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/DanceThisMeshAround.cpp:173 hands this jack to paramValue(), defined at src/controller/DanceThisMeshAround.cpp:108: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0.05 to 1, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels. Line 174 then pushes a band up if it comes within 20 per cent of the band below it."},
      "4": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/DanceThisMeshAround.cpp:173 hands this jack to paramValue(), defined at src/controller/DanceThisMeshAround.cpp:108: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0.05 to 1, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels. Line 174 then pushes a band up if it comes within 20 per cent of the band below it."},
      "5": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/DanceThisMeshAround.cpp:173 hands this jack to paramValue(), defined at src/controller/DanceThisMeshAround.cpp:108: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0.05 to 1, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels. Line 174 then pushes a band up if it comes within 20 per cent of the band below it."},
      "6": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "stepped", "negative": "subtracts", "why": "src/controller/DanceThisMeshAround.cpp:226 hands this jack to paramValue(), defined at src/controller/DanceThisMeshAround.cpp:108: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 1 to 500, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels. The result is stored in an int array of delay times in samples, so it steps by one sample."},
      "7": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "stepped", "negative": "subtracts", "why": "src/controller/DanceThisMeshAround.cpp:226 hands this jack to paramValue(), defined at src/controller/DanceThisMeshAround.cpp:108: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 1 to 500, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels. The result is stored in an int array of delay times in samples, so it steps by one sample."},
      "8": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "stepped", "negative": "subtracts", "why": "src/controller/DanceThisMeshAround.cpp:237 hands this jack to paramValue(), defined at src/controller/DanceThisMeshAround.cpp:108: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 1 to 500, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels. The result is doubled and stored in an int, so it steps by two samples."},
      "11": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/DanceThisMeshAround.cpp:238 hands this jack to paramValue(), defined at src/controller/DanceThisMeshAround.cpp:108: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0 to 0.95, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "12": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/DanceThisMeshAround.cpp:239 hands this jack to paramValue(), defined at src/controller/DanceThisMeshAround.cpp:108: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 1 to 5, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "14": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/DanceThisMeshAround.cpp:210 hands this jack to paramValue(), defined at src/controller/DanceThisMeshAround.cpp:108: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0 to 1, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels."}
    },
    "outputs": {
      "0": {"family": "audio", "poly": false, "why": "src/controller/DanceThisMeshAround.cpp:429 writes the waveguide and mesh sum times 5 with no clamp, so no range can be stated. setChannels() is never called, so the jack stays monophonic."}
    }
  },
  "comment": "Four parameters and four inputs are declared with no widget on the panel (mesh impedance, the two waveguide feedback CVs and an allpass cutoff CV), so they can never be clicked and are left untagged. The second output is declared and never drawn."
}
