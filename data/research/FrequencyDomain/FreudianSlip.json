{
  "plugin": "FrequencyDomain",
  "module": "FreudianSlip",
  "facts": {
    "inputs": {
      "0": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "negative": "ignored", "why": "src/controller/FreudianSlip.cpp:517 passes the jack to a dsp::SchmittTrigger, whose Rack defaults fire above 1V and reset below 0V, and a rising edge starts playback. getVoltage() reads channel 1 only."},
      "2": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/FreudianSlip.cpp:552 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftX, and src/model/Cells.hpp:334 adds that figure times the grid’s full value range to every band. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "3": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/FreudianSlip.cpp:553 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftY, and src/model/Cells.hpp:309 adds that figure times the grid’s height to every band position. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "4": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/FreudianSlip.cpp:557 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftX, and src/model/Cells.hpp:334 adds that figure times the grid’s full value range to every band. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "5": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/FreudianSlip.cpp:558 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftY, and src/model/Cells.hpp:309 adds that figure times the grid’s height to every band position. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "6": {"family": "audio", "poly": true, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/FreudianSlip.cpp:765 loops to this jack’s getChannels() and reads getVoltage(i) per channel; the README at line 90 says the frequency modulation inputs are polyphonic and that the two jacks together carry up to 32. Line 767 multiplies each channel by 2000, the maker’s comment giving ±5V as ±10000Hz."},
      "7": {"family": "audio", "poly": true, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/FreudianSlip.cpp:770 loops to this jack’s getChannels() and reads getVoltage(i) per channel; the README at line 90 says the frequency modulation inputs are polyphonic and that the two jacks together carry up to 32. Line 772 multiplies each channel by 2000, the maker’s comment giving ±5V as ±10000Hz. Its channels are counted on from the first FM jack’s."},
      "8": {"family": "audio", "poly": true, "range": "0 to 5V", "step": "continuous", "negative": "ignored", "why": "src/controller/FreudianSlip.cpp:779 loops to this jack’s getChannels() and reads getVoltage(i) per channel; the README at line 94 says the RM/AM inputs are polyphonic and that the two jacks together carry up to 32. src/model/OscillatorBank.hpp:66 divides each channel by 5, and line 180 drops anything below 0 before multiplying, so a negative voltage silences rather than inverts."},
      "9": {"family": "audio", "poly": true, "range": "0 to 5V", "step": "continuous", "negative": "ignored", "why": "src/controller/FreudianSlip.cpp:784 loops to this jack’s getChannels() and reads getVoltage(i) per channel; the README at line 94 says the RM/AM inputs are polyphonic and that the two jacks together carry up to 32. src/model/OscillatorBank.hpp:66 divides each channel by 5, and line 180 drops anything below 0 before multiplying. Its channels are counted on from the first AM/RM jack’s."},
      "10": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "stepped", "negative": "subtracts", "why": "src/controller/FreudianSlip.cpp:536 hands this jack to paramValue(), defined at src/controller/FreudianSlip.cpp:309: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 1 to 32, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels. The result is cast to a uint8_t, so it steps voice by voice."},
      "11": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "stepped", "negative": "subtracts", "why": "src/controller/FreudianSlip.cpp:546 hands this jack to paramValue(), defined at src/controller/FreudianSlip.cpp:309: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0 to 4, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels. The result is cast to a uint8_t, so it steps through the five waveforms."},
      "12": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/FreudianSlip.cpp:541 hands this jack to paramValue(), defined at src/controller/FreudianSlip.cpp:309: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans -3 to 3, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels. Line 543 then turns that figure into a rate multiplier of 1 plus it above zero and one over 1 minus it below, which matches an octave only at 1 and −1."},
      "13": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/FreudianSlip.cpp:626 hands this jack to paramValue(), defined at src/controller/FreudianSlip.cpp:309: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0 to 1, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "14": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/FreudianSlip.cpp:627 hands this jack to paramValue(), defined at src/controller/FreudianSlip.cpp:309: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0 to 1, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "15": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/FreudianSlip.cpp:563 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftX, and src/model/Cells.hpp:334 adds that figure times the grid’s full value range to every band. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "16": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/FreudianSlip.cpp:564 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftY, and src/model/Cells.hpp:309 adds that figure times the grid’s height to every band position. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "17": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/FreudianSlip.cpp:568 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftX, and src/model/Cells.hpp:334 adds that figure times the grid’s full value range to every band. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "18": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/FreudianSlip.cpp:569 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftY, and src/model/Cells.hpp:309 adds that figure times the grid’s height to every band position. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "19": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "src/controller/FreudianSlip.cpp:623 hands this jack to paramValue(), defined at src/controller/FreudianSlip.cpp:309: the jack’s voltage divided by 10 and multiplied by the knob’s full span is added to the knob, and the total is clamped to the knob’s own limits. This call spans 0 to 1, so 10V covers the whole span and a negative voltage subtracts. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "20": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/FreudianSlip.cpp:598 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftX, and src/model/Cells.hpp:334 adds that figure times the grid’s full value range to every band. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "21": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/FreudianSlip.cpp:599 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftY, and src/model/Cells.hpp:309 adds that figure times the grid’s height to every band position. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "22": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/FreudianSlip.cpp:600 reads this jack as getVoltage()/5.0 and stores it as the grid’s rotateX, and src/model/Cells.hpp:353 takes the cosine of pi times that figure and tilts the drawn values by it. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "23": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/FreudianSlip.cpp:590 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftX, and src/model/Cells.hpp:334 adds that figure times the grid’s full value range to every band. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "24": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/FreudianSlip.cpp:591 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftY, and src/model/Cells.hpp:309 adds that figure times the grid’s height to every band position. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "25": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/FreudianSlip.cpp:592 reads this jack as getVoltage()/5.0 and stores it as the grid’s rotateX, and src/model/Cells.hpp:353 takes the cosine of pi times that figure and tilts the drawn values by it. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "26": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/FreudianSlip.cpp:574 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftX, and src/model/Cells.hpp:334 adds that figure times the grid’s full value range to every band. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "27": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/FreudianSlip.cpp:575 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftY, and src/model/Cells.hpp:309 adds that figure times the grid’s height to every band position. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "28": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/FreudianSlip.cpp:576 reads this jack as getVoltage()/5.0 and stores it as the grid’s rotateX, and src/model/Cells.hpp:353 takes the cosine of pi times that figure and tilts the drawn values by it. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "29": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/FreudianSlip.cpp:582 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftX, and src/model/Cells.hpp:334 adds that figure times the grid’s full value range to every band. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "30": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/FreudianSlip.cpp:583 reads this jack as getVoltage()/5.0 and stores it as the grid’s shiftY, and src/model/Cells.hpp:309 adds that figure times the grid’s height to every band position. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "31": {"family": "cv", "poly": false, "range": "±5V", "step": "continuous", "negative": "swings", "why": "src/controller/FreudianSlip.cpp:584 reads this jack as getVoltage()/5.0 and stores it as the grid’s rotateX, and src/model/Cells.hpp:353 takes the cosine of pi times that figure and tilts the drawn values by it. Nothing clamps it, so 5V is the whole sweep and a negative voltage moves the other way. getVoltage() reads channel 1, and nothing loops over this jack’s channels."},
      "32": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "negative": "ignored", "why": "src/controller/FreudianSlip.cpp:496 adds this jack’s voltage to the LOOP button and passes the total to a dsp::SchmittTrigger, whose Rack defaults fire above 1V and reset below 0V. Because the button is added rather than tested separately, a cable held above 1V keeps the trigger high and the button then cannot toggle. getVoltage() reads channel 1 only."}
    },
    "outputs": {
      "0": {"family": "audio", "poly": false, "why": "src/controller/FreudianSlip.cpp:804 and 807 write the oscillator bank sum with no clamp, so no range can be stated. setChannels() is never called."},
      "1": {"family": "audio", "poly": false, "why": "src/controller/FreudianSlip.cpp:803 tests this jack for isConnected(): with nothing patched here the module sums to mono on the left jack alone, and only when a cable is in do lines 808 and 811 write the right side."},
      "2": {"family": "trigger", "poly": false, "range": "0 to 10V", "step": "stepped", "why": "src/controller/FreudianSlip.cpp:816 writes 10V while a pulse triggered at line 693 for 1ms is running and 0V otherwise. setChannels() is never called."}
    }
  },
  "comment": "The TUNING input is named V/Oct by the maker but is read like every other CV on the plugin: it adds a tenth of its voltage times the parameter range, so one volt adds 0.6 to a -3 to 3 setting. A ring modulation parameter, a second signal input and a fourth output are declared with no widget on the panel."
}
