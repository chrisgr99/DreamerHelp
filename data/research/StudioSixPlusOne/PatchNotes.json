{
  "plugin": "StudioSixPlusOne",
  "module": "PatchNotes",
  "comment": "The text field is a LedDisplayTextField with multiline set to false, so it holds one line; the shipped Cable Colours preset stores its columns as spaces inside a single string."
}
