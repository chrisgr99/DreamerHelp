{
  "plugin": "StudioSixPlusOne",
  "module": "Mix",
  "comment": "The README calls the mixer AC coupled; the source has the DC blocking filter commented out with the note that this lets CV pass, and the panel says nothing either way. The entry follows the code. The NLD TYPE slider prints the parameter name and the shape name together, so the curve is named on the slider itself."
}
