{
  "plugin": "SickoCV",
  "module": "SampleDelay",
  "facts": {
    "inputs": {
      "0": {"poly": false, "step": "continuous", "why": "SampleDelay.cpp:53 getVoltage(); the file contains no getPolyVoltage and no getChannels"},
      "1": {"poly": false, "step": "continuous", "normal": "the jack above it", "why": "SampleDelay.cpp:55 isConnected() test, :61 falls back to delayBuffer[t-1] when unpatched"},
      "2": {"poly": false, "step": "continuous", "normal": "the jack above it", "why": "SampleDelay.cpp:55 isConnected() test, :61 falls back to delayBuffer[t-1] when unpatched"}
    },
    "outputs": {
      "0": {"poly": false, "why": "SampleDelay.cpp:68 setVoltage() with no setChannels anywhere in the file"},
      "1": {"poly": false, "why": "SampleDelay.cpp:68 setVoltage() with no setChannels anywhere in the file"},
      "2": {"poly": false, "why": "SampleDelay.cpp:68 setVoltage() with no setChannels anywhere in the file"}
    }
  },
  "comment": "The normalling at SampleDelay.cpp:61 copies the previous row's undelayed input sample, not its delayed output, so the three rows do not chain into a longer delay. Ports carry whatever is patched, so no family is set."
}
