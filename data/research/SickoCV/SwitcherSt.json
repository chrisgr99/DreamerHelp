{
  "plugin": "SickoCV",
  "module": "SwitcherSt",
  "facts": {
    "inputs": {
      "0": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "SwitcherSt.cpp:189 getVoltage() compared against 1V, as a level in Gate mode and as a rising edge in Toggle mode"},
      "1": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "SwitcherSt.cpp takes this jack as a rising edge at 1V wherever a reset is handled"},
      "2": {"poly": true, "step": "continuous", "why": "SwitcherSt.cpp takes getChannels() from this jack as the loop bound, with getVoltage(c) read inside it"},
      "3": {"poly": true, "step": "continuous", "why": "SwitcherSt.cpp takes getChannels() from this jack as the loop bound, with getVoltage(c) read inside it"},
      "4": {"poly": true, "step": "continuous", "why": "SwitcherSt.cpp takes getChannels() from this jack as the loop bound, with getVoltage(c) read inside it"},
      "5": {"poly": true, "step": "continuous", "why": "SwitcherSt.cpp takes getChannels() from this jack as the loop bound, with getVoltage(c) read inside it"},
      "6": {"family": "cv", "poly": false, "step": "continuous", "negative": "subtracts", "sumRange": "0 to 10V", "why": "SwitcherSt.cpp:207 getVoltage() added to the knob as a time in seconds, the total clamped to 0-10 seconds at :271-273"}
    },
    "outputs": {
      "0": {"poly": true, "why": "SwitcherSt.cpp sets the channel count from whichever input is feeding it, with setVoltage(value, c) in the loop above"},
      "1": {"poly": true, "why": "SwitcherSt.cpp sets the channel count from whichever input is feeding it, with setVoltage(value, c) in the loop above"},
      "2": {"poly": true, "why": "SwitcherSt.cpp sets the channel count from whichever input is feeding it, with setVoltage(value, c) in the loop above"},
      "3": {"poly": true, "why": "SwitcherSt.cpp sets the channel count from whichever input is feeding it, with setVoltage(value, c) in the loop above"}
    }
  },
  "comment": "The FADE CV sumRange is a time in seconds, not a voltage. Unlike Switcher, this version has no Route & Hold menu item. The IN and OUT jacks carry whatever is patched, so they get no family."
}
