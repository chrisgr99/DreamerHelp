{
  "plugin": "VectorModular",
  "module": "SoloMixer",
  "facts": {
    "inputs": {
      "0": {"poly": false, "step": "continuous", "negative": "swings", "normal": "5V", "why": "src/SoloMixer.cpp:237 reads one channel with getVoltage when connected and substitutes a constant 5.0f when not; :242 multiplies by the squared knob and :244-246 shape and clamp the result"},
      "1": {"poly": false, "step": "continuous", "negative": "swings", "normal": "the red channel before its level knob", "why": "src/SoloMixer.cpp:238 reads one channel with getVoltage when connected and otherwise copies redSignal, which at that line has not yet had the red gain of :242 applied"},
      "2": {"poly": false, "step": "continuous", "negative": "swings", "normal": "the green channel before its level knob", "why": "src/SoloMixer.cpp:239 reads one channel with getVoltage when connected and otherwise copies greenSignal, which at that line has not yet had the green gain of :248 applied"}
    },
    "outputs": {
      "0": {"poly": false, "range": "±10V", "step": "continuous", "why": "src/SoloMixer.cpp:246 clamps to -10V and 10V after the shaper at :244, and :450 writes one channel with setVoltage; :404 forces it to 0V when another channel is soloed"},
      "1": {"poly": false, "range": "±10V", "step": "continuous", "why": "src/SoloMixer.cpp:250 clamps to -10V and 10V after the shaper at :249, and :451 writes one channel with setVoltage; :408 forces it to 0V when another channel is soloed"},
      "2": {"poly": false, "range": "±10V", "step": "continuous", "why": "src/SoloMixer.cpp:254 clamps to -10V and 10V after the shaper at :253, and :452 writes one channel with setVoltage; :412 forces it to 0V when another channel is soloed"},
      "3": {"poly": false, "range": "±10V", "step": "continuous", "why": "src/SoloMixer.cpp:434 multiplies the sum of the three channels by the -1.5 to 1.5 knob; :437-438 shape it at a 10V threshold and clamp to -10V and 10V; :453 writes one channel"},
      "4": {"poly": false, "range": "±10V", "step": "continuous", "why": "src/SoloMixer.cpp:435 multiplies the sum of the three channels by the absolute value of the same knob; :440-441 shape and clamp to -10V and 10V; :454 writes one channel"}
    }
  },
  "comment": "No family is set on any jack. The maker's own page says the head channel normals to 5V so the module can be used as a CV offset, and the plugin.json tags are Mixer, Controller and Attenuator — a channel here carries audio or CV depending on what you patch, so a colour would be a guess.\n\nThe soft clipper is not a soft clipper. softClip at src/SoloMixer.cpp:166-180 returns the signal unchanged below the threshold, and above it returns threshold * (0.5 + 0.5 * cos(pi/2 * (|x| - T) / T)). That expression equals T at |x| = T, falls to T/2 at |x| = 2T, reaches zero at |x| = 3T and turns negative beyond. For the channel outputs T is 5V, so an input at 10V with the level knob at its top (gain 1.5) produces 15V into the shaper and 0V out of the jack; for the two mix outputs T is 10V and the same collapse happens at 30V. The maker's page describes it as \"Soft-clipping overdrive on each output\". The line on each output carries the short form of this; nothing else in the module limits the level.\n\nThe maker's page calls the channel level control logarithmic. The code at :242, :248 and :252 raises the knob position to the second power, which is the opposite curve: the gain moves slowly at the bottom of the travel and fastest near the top. The knob's configured range is sqrt(1.5), so the squared gain runs 0 to 1.5 exactly.\n\nThe four solo handlers each contain a bare `return;` (lines 262, 280, 298, 316) that would abandon the rest of process. It is unreachable: a dsp::SchmittTrigger returns true only on a rising edge, so the guarded lastState flag is never true on the sample the trigger fires. No effect, but it is the kind of early return that would silence the module if the trigger behaviour ever changed.\n\nTwelve of the lights are TinyLight indicators for the solo and bank states. They are LightWidgets, not parameters, so they take no tag.\n\nRead at commit 6cda28c; see the plugin-level note under baseTrigs for how the commit was settled."
}
