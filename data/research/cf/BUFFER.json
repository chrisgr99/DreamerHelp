{
  "plugin": "cf",
  "module": "BUFFER",
  "facts": {
    "inputs": {
      "0": {"poly": false, "step": "continuous", "why": "cf 2.0.2 src/BUFFER.cpp:95: getVoltage() on this port, written into the delay line; no file in the plugin calls getPolyVoltage or getChannels"},
      "1": {"poly": false, "step": "continuous", "why": "cf 2.0.2 src/BUFFER.cpp:95: getVoltage() on this port, added to the delay line through the FB knob; no file in the plugin calls getPolyVoltage or getChannels"},
      "2": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "stepped", "why": "cf 2.0.2 src/BUFFER.cpp:88: clamp(int(getVoltage()*999.8),0,9998)+1 sets the buffer length in samples, and the lamp reads clamp(getVoltage(),0,10); no file in the plugin calls getPolyVoltage or getChannels"}
    }
  }
}
