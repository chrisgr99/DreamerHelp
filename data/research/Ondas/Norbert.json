{
  "plugin": "Ondas",
  "module": "Norbert",
  "facts": {
    "inputs": {
      "0": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "Norbert.cpp Norbert::process: a Rack SchmittTrigger at its default 0.1V and 1V thresholds on getVoltage() with no channel argument, commit 05c0768, tagged v2.1.0, whose plugin.json carries the installed 2.1.0 and whose res/ matches the installed panels"},
      "1": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "Norbert.cpp Norbert::process: kCV = (getVoltage() * attenuator) / 10.f then clamp(knob + kCV, 0.0f, 1.0f), so a negative voltage pulls the setting down to its 0 floor, commit 05c0768, tagged v2.1.0, whose plugin.json carries the installed 2.1.0 and whose res/ matches the installed panels"},
      "2": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "subtracts", "why": "Norbert.cpp Norbert::process: dCV = (getVoltage() * attenuator) / 10.f then clamp(knob + dCV, 0.0f, 1.0f), so a negative voltage pulls the setting down to its 0 floor, commit 05c0768, tagged v2.1.0, whose plugin.json carries the installed 2.1.0 and whose res/ matches the installed panels"}
    },
    "outputs": {
      "0": {"family": "cv", "poly": false, "range": "±10V", "step": "continuous", "why": "Norbert.cpp Norbert::process: setVoltage(clamp(angle, -10.0f, 10.0f)) with no channel argument, commit 05c0768, tagged v2.1.0, whose plugin.json carries the installed 2.1.0 and whose res/ matches the installed panels"}
    }
  },
  "comment": "The spring constant is k = 5 * (1000/5)^kSum and the damping is the 0 to 1 setting times 10; the impulse is scaled by (5/k)^0.1 so a stiffer spring gets a slightly smaller kick. A member named springFactor is declared and never used."
}
