{
  "plugin": "Kilpatrick-Toolbox",
  "module": "MIDI_Monitor",
  "facts": {
    "inputs": {
      "0": {"poly": false, "why": "MIDI_Monitor.cpp:81 wraps each input in a CVMidi reader, which calls Port::getVoltage at utils/CVMidi.h:71 and so reads channel 1 only"},
      "1": {"poly": false, "why": "MIDI_Monitor.cpp:81 wraps each input in a CVMidi reader, which calls Port::getVoltage at utils/CVMidi.h:71 and so reads channel 1 only"},
      "2": {"poly": false, "why": "MIDI_Monitor.cpp:81 wraps each input in a CVMidi reader, which calls Port::getVoltage at utils/CVMidi.h:71 and so reads channel 1 only"},
      "3": {"poly": false, "why": "MIDI_Monitor.cpp:81 wraps each input in a CVMidi reader, which calls Port::getVoltage at utils/CVMidi.h:71 and so reads channel 1 only"}
    }
  },
  "comment": "The four switches are configured with a default of 0 at MIDI_Monitor.cpp:72-75, but onReset at :160 sets every one to 1 and the constructor calls onReset, so a new module comes up with all four inputs showing. The buttons are made non-momentary at :226, :231, :236 and :241. The line format is at :114-130, seven lines are held (DISPLAY_LINES at :60) and the running count wraps at 8192 (:180)."
}
