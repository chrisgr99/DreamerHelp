{
  "plugin": "Kilpatrick-Toolbox",
  "module": "MIDI_Channel",
  "facts": {
    "inputs": {
      "0": {"poly": false, "why": "MIDI_Channel.cpp:79 wraps this input in a CVMidi reader, which calls Port::getVoltage at utils/CVMidi.h:71 and so reads channel 1 only"}
    },
    "outputs": {
      "0": {"poly": false, "why": "MIDI_Channel.cpp:80 wraps this output in a CVMidi writer, which calls Port::setVoltage at utils/CVMidi.h:140 and so writes channel 1 only"},
      "1": {"poly": false, "why": "MIDI_Channel.cpp:81 wraps this output in a CVMidi writer, which calls Port::setVoltage at utils/CVMidi.h:140 and so writes channel 1 only"}
    }
  },
  "comment": "Five parameters are declared and configured but none is given a ParamWidget: MIDI_Channel.cpp:264-314 builds four KilpatrickLabel displays that drive them by hover-scroll and double-click instead. The census confirms an empty paramPos, so no click can reach any of them and none gets a line; the four Note lines carry them instead. Key split routing is at MIDI_Channel.cpp:134-139, the output channel restamp at :129, transpose at :141 and the note-off flush at :106-116."
}
