{
  "plugin": "Coffee",
  "module": "Fork2",
  "facts": {
    "inputs": {
      "0": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "Fork2.cpp Fork2::process: a Rack SchmittTrigger at its default thresholds; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "1": {"family": "cv", "poly": false, "range": "±10V", "step": "continuous", "negative": "swings", "why": "Fork2.cpp Fork2::process: the jack replaces the threshold knob, whose range is -10V to 10V; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "2": {"family": "cv", "poly": false, "step": "continuous", "why": "Fork2.cpp Fork2::process: the jack has the offset added and is multiplied by the scale knob; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "3": {"family": "cv", "poly": false, "step": "continuous", "why": "Fork2.cpp Fork2::process: the jack has the offset added and is multiplied by the scale knob; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "4": {"family": "cv", "poly": false, "step": "continuous", "why": "Fork2.cpp Fork2::process: the jack is sampled and compared against the threshold; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"}
    },
    "outputs": {
      "0": {"family": "cv", "poly": false, "step": "continuous", "why": "Fork2.cpp Fork2::process: one setVoltage with no channel argument, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "1": {"family": "trigger", "poly": false, "range": "0 to 10V", "step": "stepped", "why": "Fork2.cpp Fork2::process: a 1ms pulse generator writing 10V, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "2": {"family": "trigger", "poly": false, "range": "0 to 10V", "step": "stepped", "why": "Fork2.cpp Fork2::process: a 1ms pulse generator writing 10V, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"}
    }
  },
  "comment": "The whole output stage sits inside a test for the sampled source having changed, so a still source freezes the module against every knob on the panel."
}
