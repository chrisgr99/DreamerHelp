{
  "plugin": "Coffee",
  "module": "Some",
  "facts": {
    "inputs": {
      "0": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "Some.cpp Some::process: a Rack SchmittTrigger at its default thresholds; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "1": {"family": "cv", "poly": false, "range": "0 to 10V", "step": "continuous", "negative": "ignored", "why": "Some.cpp Some::process: the jack is clamped to 0 to 1 and replaces the probability knob; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "2": {"poly": false, "step": "continuous", "why": "Some.cpp Some::process: the jack is copied straight to the matching output; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "3": {"poly": false, "step": "continuous", "why": "Some.cpp Some::process: the jack is copied straight to the matching output; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "4": {"poly": false, "step": "continuous", "why": "Some.cpp Some::process: the jack is copied straight to the matching output; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "5": {"poly": false, "step": "continuous", "why": "Some.cpp Some::process: the jack is copied straight to the matching output; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "6": {"poly": false, "step": "continuous", "why": "Some.cpp Some::process: the jack is copied straight to the matching output; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "7": {"poly": false, "step": "continuous", "why": "Some.cpp Some::process: the jack is copied straight to the matching output; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "8": {"poly": false, "step": "continuous", "why": "Some.cpp Some::process: the jack is copied straight to the matching output; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "9": {"poly": false, "step": "continuous", "why": "Some.cpp Some::process: the jack is copied straight to the matching output; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"}
    }
  },
  "comment": "The eight signal jacks carry whatever you patch into them and are copied unchanged, so no port colour can say what belongs there."
}
