{
  "plugin": "Coffee",
  "module": "Fork",
  "facts": {
    "inputs": {
      "0": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "Fork.cpp Fork::process: a Rack SchmittTrigger at its default thresholds; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "1": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "Fork.cpp Fork::process: a Rack SchmittTrigger at its default thresholds; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "2": {"poly": false, "step": "continuous", "why": "Fork.cpp Fork::process: the winning jack is copied straight to the output; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "3": {"poly": false, "step": "continuous", "why": "Fork.cpp Fork::process: the winning jack is copied straight to the output; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "4": {"poly": false, "step": "continuous", "why": "Fork.cpp Fork::process: the winning jack is copied straight to the output; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "5": {"poly": false, "step": "continuous", "why": "Fork.cpp Fork::process: the winning jack is copied straight to the output; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "6": {"family": "cv", "poly": false, "range": "±1V", "step": "continuous", "negative": "swings", "why": "Fork.cpp Fork::process: the jack replaces the chance knob and is compared against a random number in -1 to 1; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "7": {"family": "cv", "poly": false, "range": "±1V", "step": "continuous", "negative": "swings", "why": "Fork.cpp Fork::process: the jack replaces the chance knob and is compared against a random number in -1 to 1; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"}
    },
    "outputs": {
      "0": {"poly": false, "step": "continuous", "why": "Fork.cpp Fork::process: one setVoltage with no channel argument, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "1": {"poly": false, "step": "continuous", "why": "Fork.cpp Fork::process: one setVoltage with no channel argument, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"}
    }
  },
  "comment": "The A and B jacks are copied unchanged, so no port colour can say what belongs there. The ready flag is cleared whenever the trigger jack reads anything other than exactly 0V, which is why a held-high trigger blocks the button."
}
