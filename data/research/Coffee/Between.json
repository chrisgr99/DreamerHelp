{
  "plugin": "Coffee",
  "module": "Between",
  "facts": {
    "inputs": {
      "0": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "Between.cpp Between::process: a Rack SchmittTrigger at its default 0.1V and 1V thresholds; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "1": {"family": "cv", "poly": false, "step": "continuous", "why": "Between.cpp Between::process: the jack is added to the offset knob and multiplied by the scale knob; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "2": {"family": "cv", "poly": false, "step": "continuous", "why": "Between.cpp Between::process: the jack is added to the offset knob and multiplied by the scale knob; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"}
    },
    "outputs": {
      "0": {"family": "cv", "poly": false, "step": "continuous", "why": "Between.cpp Between::process: one setVoltage with no channel argument, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"}
    }
  },
  "comment": "process() returns immediately when the output is unpatched, so no trigger is acted on and the lights and value do not move."
}
