{
  "plugin": "Coffee",
  "module": "Some3",
  "facts": {
    "inputs": {
      "0": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "Some3.cpp Some3::process: a Rack SchmittTrigger at its default thresholds; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "1": {"family": "cv", "poly": false, "range": "0 to 1V", "step": "continuous", "negative": "ignored", "why": "Some3.cpp Some3::process: the jack is clamped to 0 to 1 and replaces the probability knob; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "2": {"family": "cv", "poly": false, "step": "stepped", "negative": "ignored", "why": "Some3.cpp Some3::process: the jack is rescaled from 0 to the scaling switch value onto 0 to 16 channels; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "3": {"family": "cv", "poly": false, "step": "stepped", "negative": "ignored", "why": "Some3.cpp Some3::process: the jack is rescaled from 0 to the scaling switch value onto 0 to 16 channels; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "4": {"poly": true, "step": "continuous", "why": "Some3.cpp Some3::getOutput: the jack is read per channel with getVoltage(channel) when it is polyphonic, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "5": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "Some3.cpp Some3::process: a Rack SchmittTrigger at its default thresholds; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "6": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "Some3.cpp Some3::process: a Rack SchmittTrigger at its default thresholds; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"},
      "7": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "why": "Some3.cpp Some3::process: a Rack SchmittTrigger at its default thresholds; every voltage on this module is read with getVoltage() and no channel argument, so only the first channel of a cable is used, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"}
    },
    "outputs": {
      "0": {"poly": true, "step": "continuous", "why": "Some3.cpp Some3::process: setChannels(16) is called on the output and each channel is written separately, commit dfb2174, the 2.4.1 release merge, whose res/ matches the installed panels"}
    }
  },
  "comment": "An input cable with fewer channels than the block reaching into it gives 10V on the channels beyond its own count. The shift buttons move both the start and end knobs together, so the block keeps its length."
}
