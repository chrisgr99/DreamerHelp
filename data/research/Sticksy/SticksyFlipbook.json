{
  "plugin": "Sticksy",
  "module": "SticksyFlipbook",
  "facts": {
    "inputs": {
      "0": {"family": "trigger", "poly": false, "step": "stepped", "range": "high above 1V", "why": "src/plugin.cpp:535 passes the voltage to a dsp::SchmittTrigger, whose default thresholds are 0V and 1V, and reads it with getVoltage() and no channel index"},
      "1": {"family": "trigger", "poly": false, "step": "stepped", "range": "high above 1V", "why": "src/plugin.cpp:531 passes the voltage to a dsp::SchmittTrigger, whose default thresholds are 0V and 1V, and reads it with getVoltage() and no channel index"}
    },
    "outputs": {
      "0": {"family": "trigger", "poly": false, "range": "0 to 10V", "step": "stepped", "why": "src/plugin.cpp:582 writes 10 or 0 with no channel index from a dsp::PulseGenerator triggered for 1e-3 seconds at line 581"}
    }
  },
  "comment": "The three jacks carry no lettering on the panel; res/panels/flipbook/flipbook_12hp.svg is plain artwork, and the only names are the configInput and configOutput strings CLK, RST and EOC (src/plugin.cpp:385 to 387). The reset is tested before the clock and sets a flag that skips the clock in the same sample (src/plugin.cpp:531 to 536). MAX_FRAMES is 128 (src/plugin.cpp:362). A reset when no frames are loaded does nothing at all, because resetPlaybackToFirstFrame returns early on an empty sequence. This model has no parameters. Unlike the four blanks it carries no Storage menu item: a flipbook always references the files where they were found."
}
