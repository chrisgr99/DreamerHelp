{
  "plugin": "Stoermelder-P1",
  "module": "CVMap",
  "facts": {
    "inputs": {
      "0": {"family": "cv", "poly": true, "step": "continuous", "why": "CVMap.cpp:137 records getChannels() on it and CVMap.cpp:114 reads the mapped channel with getVoltage(c)"},
      "1": {"family": "cv", "poly": true, "step": "continuous", "why": "CVMap.cpp:137 records getChannels() on it and CVMap.cpp:114 reads the mapped channel with getVoltage(c)"}
    }
  },
  "comment": "Each channel is read as 0 to 10V by default; the Bipolar input menu option adds 5V first, which makes them -5 to 5V inputs instead."
}
