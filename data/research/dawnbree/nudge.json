{
  "plugin": "dawnbree",
  "module": "nudge",
  "facts": {
    "inputs": {
      "0": {"poly": false, "why": "src/nudge.cpp:31, a single getVoltage() with no channel loop"}
    },
    "outputs": {
      "0": {"poly": false, "why": "src/nudge.cpp:31, one setVoltage() and no setChannels()"}
    }
  },
  "comment": "The input and output carry whatever is patched in, so neither is given a family. The maker left both jacks unnamed in configInput and configOutput, so Rack's tooltip for each is empty; the panel prints input and output. Only channel 1 of a poly cable is read and only channel 1 comes out."
}
