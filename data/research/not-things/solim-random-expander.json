{
  "plugin": "not-things",
  "module": "solim-random-expander",
  "facts": {
    "inputs": {
      "0": {"family": "trigger", "poly": true, "range": "high above 1V", "step": "stepped", "normal": "0V", "why": "solim-random.cpp:128 loops over getChannels and puts each channel through m_trigTriggers[..][i].process(getVoltage(i), 0.1, 1), counting a rising edge above 1V for that voice; only the first eight channels are read"},
      "1": {"family": "trigger", "poly": true, "range": "high above 1V", "step": "stepped", "normal": "0V", "why": "solim-random.cpp:128 loops over getChannels and puts each channel through m_trigTriggers[..][i].process(getVoltage(i), 0.1, 1), counting a rising edge above 1V for that voice; only the first eight channels are read"},
      "2": {"family": "trigger", "poly": true, "range": "high above 1V", "step": "stepped", "normal": "0V", "why": "solim-random.cpp:128 loops over getChannels and puts each channel through m_trigTriggers[..][i].process(getVoltage(i), 0.1, 1), counting a rising edge above 1V for that voice; only the first eight channels are read"},
      "3": {"family": "trigger", "poly": true, "range": "high above 1V", "step": "stepped", "normal": "0V", "why": "solim-random.cpp:128 loops over getChannels and puts each channel through m_trigTriggers[..][i].process(getVoltage(i), 0.1, 1), counting a rising edge above 1V for that voice; only the first eight channels are read"}
    }
  },
  "comment": "The buttons are named Move One Up or Down, Switch Two at Random, Move All to Random Positions and Reset All in the maker's tooltips; the panel marks them with symbols rather than words."
}
