{
  "plugin": "ML_modules",
  "module": "TrigBuf",
  "facts": {
    "inputs": {
      "0": {"family": "trigger", "poly": true, "step": "stepped", "range": "high above 1V", "why": "TrigBuf.cpp:149 takes getChannels() on it and TrigBuf.cpp:178 reads each channel with getNormalPolyVoltage, summed with the ARM button, into a SchmittTrigger at its default 1V threshold"},
      "1": {"family": "trigger", "poly": true, "step": "stepped", "range": "high above 1V", "why": "TrigBuf.cpp:150 takes getChannels() on it and TrigBuf.cpp:207 reads each channel with getNormalPolyVoltage, summed with the ARM button, into a SchmittTrigger at its default 1V threshold"},
      "2": {"family": "trigger", "poly": true, "step": "stepped", "range": "high above 1V", "why": "TrigBuf.cpp:152 takes getChannels() on it and TrigBuf.cpp:175 puts each channel through a SchmittTrigger at its default 1V threshold"},
      "3": {"family": "trigger", "poly": true, "step": "stepped", "range": "high above 1V", "why": "TrigBuf.cpp:153 takes getChannels() on it and TrigBuf.cpp:204 puts each channel through a SchmittTrigger at its default 1V threshold"}
    },
    "outputs": {
      "0": {"family": "trigger"},
      "1": {"family": "trigger"}
    }
  },
  "comment": "The second ARM jack is normalled to the first, and the second GATE jack to the first, so a single arm or gate patched at the top drives both halves."
}
