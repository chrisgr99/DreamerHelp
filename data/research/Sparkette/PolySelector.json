{
  "plugin": "Sparkette",
  "module": "PolySelector",
  "facts": {
    "inputs": {
      "0": {"poly": true, "step": "continuous", "normal": "0V", "why": "src/PolySelector.cpp lines 61 and 68, getChannels and readVoltages"},
      "1": {"family": "cv", "poly": true, "range": "0 to 10V", "step": "continuous", "negative": "swings", "normal": "0V", "why": "src/PolySelector.cpp lines 77-83: s += 10 when negative, then s/10*poly_channels clamped to the channels present"}
    },
    "outputs": {
      "0": {"poly": true, "step": "continuous", "why": "src/PolySelector.cpp line 63, setChannels(select_channels)"}
    }
  },
  "comment": "src/PolySelector.cpp at commit b90cd69. The input and output carry no family because the module passes on whatever is patched. Neither jack is named by the maker; the panel prints INPUT, SELECT and OUT. With nothing patched to SELECT the output has no channels and the lamps are divided by zero, which leaves them dark."
}
