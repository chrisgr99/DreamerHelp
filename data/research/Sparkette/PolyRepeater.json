{
  "plugin": "Sparkette",
  "module": "PolyRepeater",
  "facts": {
    "inputs": {
      "0": {"poly": false, "step": "continuous", "normal": "0V", "why": "src/PolyRepeater.cpp line 42, channels[0] = inputs[IN_INPUT].getVoltage() with no channel argument"}
    },
    "outputs": {
      "0": {"poly": true, "step": "continuous", "why": "src/PolyRepeater.cpp lines 46-51, setChannels(nchan) then writeVoltages"},
      "1": {"poly": true, "step": "continuous", "why": "src/PolyRepeater.cpp lines 46-51, setChannels(nchan) then writeVoltages"},
      "2": {"poly": true, "step": "continuous", "why": "src/PolyRepeater.cpp lines 46-51, setChannels(nchan) then writeVoltages"},
      "3": {"poly": true, "step": "continuous", "why": "src/PolyRepeater.cpp lines 46-51, setChannels(nchan) then writeVoltages"}
    }
  },
  "comment": "src/PolyRepeater.cpp at commit b90cd69. Neither the input nor the outputs carry a family, because the module passes on whatever is given to it. An output is written only while a cable is patched to it (line 47), which is invisible from outside. configBypass is called for all four outputs at lines 33-36."
}
