{
  "plugin": "Sparkette",
  "module": "Trixie",
  "comment": "src/Trixie.cpp: process() advances a phase by one per second and drawLayer() draws four rectangles around the panel whose width and opacity follow sin(phase*pi), so the glow breathes once a second. Lines 54-55 widen that outline by 8HP on either side when the neighbouring module is Ellie. No parameters, inputs or outputs."
}
