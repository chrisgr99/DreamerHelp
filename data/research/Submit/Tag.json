{
  "plugin": "Submit",
  "module": "Tag",
  "comment": "The module has no parameters, inputs or outputs at all, so nothing on it can be clicked for a control note; the text field and the arrow are custom widgets. A new Tag starts with the arrow pointing right (Tag.cpp:18)."
}
