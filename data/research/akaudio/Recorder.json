{
  "plugin": "akaudio",
  "module": "Recorder",
  "comment": "The interval counter display is a custom widget, not a ParamWidget. The export menu items work whether or not a Ninjam module is adjacent; the folder items appear only when one is, because the rest of the menu returns early without a link (src/Recorder.cpp:347-350)."
}
