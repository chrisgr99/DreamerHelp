{
  "plugin": "HetrickCV",
  "module": "DataCompander",
  "facts": {
    "inputs": {
      "0": {"poly": true, "step": "continuous", "why": "DataCompander.cpp:214 loads four channels at a time across getMaxInputPolyphony()"},
      "1": {"poly": true, "step": "continuous", "why": "DataCompander.cpp:215 loads four channels at a time across getMaxInputPolyphony()"},
      "2": {"poly": true, "step": "continuous", "why": "DataCompander.cpp:203 loads four channels at a time across getMaxInputPolyphony()"},
      "3": {"poly": true, "step": "continuous", "why": "DataCompander.cpp:204 loads four channels at a time across getMaxInputPolyphony()"}
    },
    "outputs": {
      "0": {"poly": true, "why": "DataCompander.cpp:186-190 setChannels(getMaxInputPolyphony()) then a per-channel SIMD store"},
      "1": {"poly": true, "why": "DataCompander.cpp:186-190 setChannels(getMaxInputPolyphony()) then a per-channel SIMD store"},
      "2": {"poly": true, "why": "DataCompander.cpp:186-190 setChannels(getMaxInputPolyphony()) then a per-channel SIMD store"},
      "3": {"poly": true, "why": "DataCompander.cpp:186-190 setChannels(getMaxInputPolyphony()) then a per-channel SIMD store"}
    }
  },
  "comment": "The bit-depth setting scales both pairs of inputs before they are clipped, so those jacks have no single expected range."
}
