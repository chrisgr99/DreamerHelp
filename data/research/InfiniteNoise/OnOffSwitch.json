{
  "plugin": "InfiniteNoise",
  "module": "OnOffSwitch",
  "facts": {
    "inputs": {
      "0": {"family": "trigger", "poly": false, "range": "high above 1V", "step": "stepped", "negative": "ignored", "normal": "0V", "why": "OnOffSwitch.cpp:137 reads getVoltage() with no channel index, so only channel 1 is used"},
      "1": {"poly": true, "range": "±10V", "step": "continuous", "negative": "swings", "normal": "0V, leaving the ON val knob alone", "why": "OnOffSwitch.cpp:167 adds getPolyVoltage(c) times the trim knob onto the matching knob, in volts"},
      "2": {"poly": true, "range": "±10V", "step": "continuous", "negative": "swings", "normal": "0V, leaving the OFF val knob alone", "why": "OnOffSwitch.cpp:167 adds getPolyVoltage(c) times the trim knob onto the matching knob, in volts"}
    },
    "outputs": {
      "0": {"poly": true, "why": "OnOffSwitch.cpp:111 sets the output to the larger of the two value input channel counts"}
    }
  },
  "comment": "The panel prints OOS, ON/OFF, ON val. and OFF val. Setting the ON knob to 0V and the OFF knob to 10V with the input in gate mode makes the module a gate inverter, which the maker suggests."
}
