{
  "plugin": "SmarTAZZStudio-Free",
  "module": "D-Octer",
  "facts": {
    "inputs": {
      "0": {"family": "pitch", "poly": true, "range": "1V per octave", "step": "continuous", "negative": "swings", "why": "D-Octer.cpp D_Octer::process: getChannels() sets the channel count on all four outputs and getVoltage(cur_Channel) reads each channel in a loop; the voltage is used by simple addition, with no clamp, commit 55aa542, the 2.4.1 release commit, whose plugin.json carries the installed 2.4.1 and whose res/ matches the installed panels"}
    },
    "outputs": {
      "0": {"family": "pitch", "poly": true, "range": "1V per octave", "step": "continuous", "why": "D-Octer.cpp D_Octer::process: setChannels(num_Channels) then setVoltage(volt_In +/- 1.0 or 2.0, cur_Channel), commit 55aa542, the 2.4.1 release commit, whose plugin.json carries the installed 2.4.1 and whose res/ matches the installed panels"},
      "1": {"family": "pitch", "poly": true, "range": "1V per octave", "step": "continuous", "why": "D-Octer.cpp D_Octer::process: setChannels(num_Channels) then setVoltage(volt_In +/- 1.0 or 2.0, cur_Channel), commit 55aa542, the 2.4.1 release commit, whose plugin.json carries the installed 2.4.1 and whose res/ matches the installed panels"},
      "2": {"family": "pitch", "poly": true, "range": "1V per octave", "step": "continuous", "why": "D-Octer.cpp D_Octer::process: setChannels(num_Channels) then setVoltage(volt_In +/- 1.0 or 2.0, cur_Channel), commit 55aa542, the 2.4.1 release commit, whose plugin.json carries the installed 2.4.1 and whose res/ matches the installed panels"},
      "3": {"family": "pitch", "poly": true, "range": "1V per octave", "step": "continuous", "why": "D-Octer.cpp D_Octer::process: setChannels(num_Channels) then setVoltage(volt_In +/- 1.0 or 2.0, cur_Channel), commit 55aa542, the 2.4.1 release commit, whose plugin.json carries the installed 2.4.1 and whose res/ matches the installed panels"}
    }
  },
  "comment": "process() returns immediately when the input has no channels, leaving the outputs at their last written voltages."
}
