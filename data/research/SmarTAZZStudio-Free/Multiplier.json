{
  "plugin": "SmarTAZZStudio-Free",
  "module": "Multiplier",
  "facts": {
    "inputs": {
      "0": {"poly": true, "why": "Multiplier.cpp Multiplier::process: getChannels() on this jack sets the channel count for all four outputs, and getVoltages() copies the whole channel array, commit 55aa542, the 2.4.1 release commit, whose plugin.json carries the installed 2.4.1 and whose res/ matches the installed panels"}
    },
    "outputs": {
      "0": {"poly": true, "why": "Multiplier.cpp Multiplier::process: setChannels(num_Channels) then writeVoltages of the input's whole voltage array, commit 55aa542, the 2.4.1 release commit, whose plugin.json carries the installed 2.4.1 and whose res/ matches the installed panels"},
      "1": {"poly": true, "why": "Multiplier.cpp Multiplier::process: setChannels(num_Channels) then writeVoltages of the input's whole voltage array, commit 55aa542, the 2.4.1 release commit, whose plugin.json carries the installed 2.4.1 and whose res/ matches the installed panels"},
      "2": {"poly": true, "why": "Multiplier.cpp Multiplier::process: setChannels(num_Channels) then writeVoltages of the input's whole voltage array, commit 55aa542, the 2.4.1 release commit, whose plugin.json carries the installed 2.4.1 and whose res/ matches the installed panels"},
      "3": {"poly": true, "why": "Multiplier.cpp Multiplier::process: setChannels(num_Channels) then writeVoltages of the input's whole voltage array, commit 55aa542, the 2.4.1 release commit, whose plugin.json carries the installed 2.4.1 and whose res/ matches the installed panels"}
    }
  },
  "comment": "The jack carries whatever is patched to it, so no family is set. process() returns immediately when the input has no channels, which leaves the outputs at their last written voltages rather than at 0V."
}
