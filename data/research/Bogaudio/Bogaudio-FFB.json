{
  "plugin": "Bogaudio",
  "module": "Bogaudio-FFB",
  "unplaced": [
    "One knob per band, attenuating that band before they are mixed back together",
    "ALL, and separate mixes of the ODD and EVEN bands"
  ],
  "facts": {
    "inputs": {
      "0": {"family": "audio", "poly": true, "step": "continuous", "why": "FFB.cpp:78 channels() reads getChannels() here; :117 getVoltage(c) into the filter bank"},
      "1": {"family": "cv", "poly": true, "range": "±5V", "step": "continuous", "why": "FFB.cpp:108 clamp(getPolyVoltage(c)/5,-1,1) scaling the semitone shift"}
    },
    "outputs": {
      "0": {"family": "audio", "poly": true, "why": "FFB.cpp:130-131 setChannels(_channels) then a per-channel write; channels() takes the width from IN"},
      "1": {"family": "audio", "poly": true, "why": "FFB.cpp:132-133 setChannels(_channels) then a per-channel write; channels() takes the width from IN"},
      "2": {"family": "audio", "poly": true, "why": "FFB.cpp:134-135 setChannels(_channels) then a per-channel write; channels() takes the width from IN"}
    }
  }
}
