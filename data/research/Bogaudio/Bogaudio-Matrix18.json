{
  "plugin": "Bogaudio",
  "module": "Bogaudio-Matrix18",
  "facts": {
    "inputs": {
      "0": {"poly": true, "step": "continuous", "why": "matrix_base.cpp:139 getPolyVoltage(c) into the matrix; :90 channels() reads getChannels() on the first signal input"}
    },
    "outputs": {
      "0": {"poly": true, "why": "matrix_base.cpp:169-170 setChannels(_channels) then a per-channel write; MatrixModule::channels() takes the width from the first input"},
      "1": {"poly": true, "why": "matrix_base.cpp:169-170 setChannels(_channels) then a per-channel write; MatrixModule::channels() takes the width from the first input"},
      "2": {"poly": true, "why": "matrix_base.cpp:169-170 setChannels(_channels) then a per-channel write; MatrixModule::channels() takes the width from the first input"},
      "3": {"poly": true, "why": "matrix_base.cpp:169-170 setChannels(_channels) then a per-channel write; MatrixModule::channels() takes the width from the first input"},
      "4": {"poly": true, "why": "matrix_base.cpp:169-170 setChannels(_channels) then a per-channel write; MatrixModule::channels() takes the width from the first input"},
      "5": {"poly": true, "why": "matrix_base.cpp:169-170 setChannels(_channels) then a per-channel write; MatrixModule::channels() takes the width from the first input"},
      "6": {"poly": true, "why": "matrix_base.cpp:169-170 setChannels(_channels) then a per-channel write; MatrixModule::channels() takes the width from the first input"},
      "7": {"poly": true, "why": "matrix_base.cpp:169-170 setChannels(_channels) then a per-channel write; MatrixModule::channels() takes the width from the first input"}
    }
  }
}
