{
  "plugin": "Bogaudio",
  "module": "Bogaudio-PolyMult",
  "facts": {
    "inputs": {
      "0": {"family": "cv", "poly": true, "range": "0 to 10V", "step": "stepped", "why": "PolyMult.cpp:9 a one-channel cable is read as clamp(roundf(getVoltage()/10*cn),1,16); a polyphonic cable sets the count from getChannels() at :7 instead"},
      "1": {"poly": false, "step": "continuous", "why": "PolyMult.cpp:16 getVoltage(), which reads channel one only"}
    },
    "outputs": {
      "0": {"poly": true, "why": "PolyMult.cpp:17-25 setChannels(cn) then a per-channel write of the mono input; the CHANNELS knob or input sets the width"},
      "1": {"poly": true, "why": "PolyMult.cpp:17-25 setChannels(cn) then a per-channel write of the mono input; the CHANNELS knob or input sets the width"},
      "2": {"poly": true, "why": "PolyMult.cpp:17-25 setChannels(cn) then a per-channel write of the mono input; the CHANNELS knob or input sets the width"},
      "3": {"poly": true, "why": "PolyMult.cpp:17-25 setChannels(cn) then a per-channel write of the mono input; the CHANNELS knob or input sets the width"}
    }
  }
}
