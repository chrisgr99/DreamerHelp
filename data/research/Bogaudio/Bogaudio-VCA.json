{
  "plugin": "Bogaudio",
  "module": "Bogaudio-VCA",
  "facts": {
    "inputs": {
      "0": {"family": "cv", "poly": true, "range": "0 to 10V", "step": "continuous", "why": "VCA.cpp:32 clamp(getPolyVoltage(c)/10,0,1)"},
      "1": {"poly": true, "step": "continuous", "why": "VCA.cpp:28 this input's own getChannels() sets the row's channel count; :37 getVoltage(c) into the amplifier"},
      "2": {"family": "cv", "poly": true, "range": "0 to 10V", "step": "continuous", "why": "VCA.cpp:32 clamp(getPolyVoltage(c)/10,0,1)"},
      "3": {"poly": true, "step": "continuous", "why": "VCA.cpp:28 this input's own getChannels() sets the row's channel count; :37 getVoltage(c) into the amplifier"}
    },
    "outputs": {
      "0": {"poly": true, "why": "VCA.cpp:27-28 and 36 or 42 setChannels(that channel's input channels) then a per-channel write"},
      "1": {"poly": true, "why": "VCA.cpp:27-28 and 36 or 42 setChannels(that channel's input channels) then a per-channel write"}
    }
  }
}
