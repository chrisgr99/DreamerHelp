{
  "plugin": "Bogaudio",
  "module": "Bogaudio-Noise",
  "facts": {
    "inputs": {
      "0": {"poly": true, "step": "continuous", "why": "Noise.cpp:28 this input's own getChannels() sets the ABS output's channel count; :31 getPolyVoltage(i) rectified"}
    },
    "outputs": {
      "0": {"family": "audio", "poly": true, "range": "±10V", "why": "Noise.cpp:11-12 setChannels(_polyChannels) then a per-channel write clamped to ±10V; the Polyphony channels menu sets the width"},
      "1": {"family": "audio", "poly": true, "range": "±10V", "why": "Noise.cpp:15-16 setChannels(_polyChannels) then a per-channel write clamped to ±10V; the Polyphony channels menu sets the width"},
      "2": {"family": "audio", "poly": true, "range": "±10V", "why": "Noise.cpp:19-20 setChannels(_polyChannels) then a per-channel write clamped to ±10V; the Polyphony channels menu sets the width"},
      "3": {"family": "audio", "poly": true, "range": "±10V", "why": "Noise.cpp:23-24 setChannels(_polyChannels) then a per-channel write clamped to ±10V; the Polyphony channels menu sets the width"},
      "4": {"poly": true, "why": "Noise.cpp:29 and 35 setChannels(ABS input channels) then a per-channel write of the rectified input"},
      "5": {"family": "audio", "poly": true, "range": "±10V", "why": "Noise.cpp:7-8 setChannels(_polyChannels) then a per-channel write clamped to ±10V; the Polyphony channels menu sets the width"}
    }
  }
}
