{
  "plugin": "Bogaudio",
  "module": "Bogaudio-AddrSeq",
  "facts": {
    "inputs": {
      "0": {"family": "trigger", "poly": true, "range": "high above 1V", "step": "stepped", "why": "addressable_sequence.cpp:74 channels() reads getChannels() here by default; :96 getPolyVoltage(c) into a bogaudio Trigger, thresholds 1V and 0.1V in rack_overrides.hpp:13"},
      "1": {"family": "trigger", "poly": true, "range": "high above 1V", "step": "stepped", "why": "addressable_sequence.cpp:89 getPolyVoltage(c) into a bogaudio Trigger, thresholds 1V and 0.1V in rack_overrides.hpp:13"},
      "2": {"family": "cv", "poly": true, "range": "±9.99V", "step": "stepped", "why": "addressable_sequence.cpp:125 clamp(getPolyVoltage(c),-9.99,9.99)/10 scaled by the step count and truncated to a step index"}
    },
    "outputs": {
      "0": {"family": "cv", "poly": true, "why": "AddrSeq.cpp:39-40 setChannels(_channels) then a per-channel write; channels() takes the width from the SELECT input"}
    }
  },
  "comment": "With 'Triggered select mode' on the menu, the SELECT CV jack is read as a trigger instead: addressable_sequence.cpp:119 puts it into a bogaudio Trigger, thresholds 1V and 0.1V, and each trigger advances the selection by one step."
}
