{
  "plugin": "Noise86",
  "module": "EyeSeq",
  "facts": {
    "inputs": {
      "0": {"family": "trigger"},
      "1": {"family": "trigger"}
    },
    "outputs": {
      "0": {"family": "cv"}
    }
  },
  "comment": "The step count wraps at 720720, the lowest common multiple of 1 to 16, so the whole pattern repeats there. The output is a plain sum of offsets; the README calls the module a pitch sequencer, but nothing in it scales to a volt per octave, so the output family is left as cv."
}
