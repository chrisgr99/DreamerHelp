{
  "plugin": "DreamerMPX",
  "module": "mpxComp",
  "comment": "The param tags follow the index order in the module's ParamId enum, where Rhythm and Rate were appended after Strum and Balance rather than inserted beside Pattern; the lines are in panel order, so the map is deliberately not a run."
}
