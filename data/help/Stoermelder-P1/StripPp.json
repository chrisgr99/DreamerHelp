{
  "plugin": "Stoermelder-P1",
  "module": "StripPp",
  "description": "Imports a Rack selection file or the clipboard into the patch, keeping parameter mappings and GLUE labels intact.",
  "notes": [
    "the module takes no part in saving, works with any .vcvs file, and can be removed once the import is done."
  ],
  "menu": [
    "Import — reads a .vcvs selection file and shows the selection under the pointer, where a left-click places it and a right-click cancels; Ctrl, Shift and B does the same.",
    "Paste — places a selection held in the clipboard the same way; Ctrl, Shift and V does the same.",
    "Show preview — draws the selection under the pointer before it is placed.",
    "Recent selections — lists the selection files imported before, for importing one again."
  ]
}
