{
  "plugin": "DHE-Modules",
  "module": "Func6",
  "description": "Six chained knobs with selectable ranges, each offsetting or scaling a signal",
  "params": [
    {"ids": [0, 1, 2, 3, 4, 5], "text": "Sets the amount added to or multiplied by that channel's input, within the range currently chosen"},
    {"ids": [6, 7, 8, 9, 10, 11], "text": "Chooses the function applied to the input: ADD offsets it by adding the knob's voltage, MULT scales it by multiplying by the knob's value. Each function has its own set of knob ranges"},
    {"ids": [12, 13, 14, 15, 16, 17], "text": "Cycles the range of the knob while ADD is chosen: 0-5V, -5 to 5V, 0-10V or -10 to 10V"},
    {"ids": [18, 19, 20, 21, 22, 23], "text": "Cycles the range of the knob while MULT is chosen: 0 to 1, -1 to 1, 0 to 2, -2 to 2, 0 to 5, -5 to 5, 0 to 10, or -10 to 10"}
  ],
  "inputs": [
    {"ids": [0, 1, 2, 3, 4, 5], "text": "The signal that channel's function is applied to; with nothing patched it takes the output of the channel above, and the top channel takes 0V"}
  ],
  "outputs": [
    {"ids": [0, 1, 2, 3, 4, 5], "text": "That channel's input after its offset or scaling, and the signal the channel below takes when its own input is unpatched"}
  ],
  "notes": [
    "the module is mono: it reads the first channel of a polyphonic cable and ignores the rest, and every output carries a single channel"
  ],
  "expects": {
    "inputs": {
      "0": "continuous · one channel only",
      "1": "continuous · one channel only",
      "2": "continuous · one channel only",
      "3": "continuous · one channel only",
      "4": "continuous · one channel only",
      "5": "continuous · one channel only"
    }
  }
}
