{
  "plugin": "RPJ",
  "module": "Gaza",
  "description": "An envelope follower driving the cutoff of a second order low pass filter, so that the louder the input the higher the filter opens.",
  "params": [
    {"ids": [0], "text": "Sets the fall time of the detector, 1ms to 2 seconds, starting at 500ms; the slope is measured in dB."},
    {"ids": [1], "text": "Sets the rise time of the detector, 1ms to 250ms, starting at 20ms; the slope is measured in dB."},
    {"ids": [2], "text": "Sets the cutoff the filter rests at while the input stays under the threshold, 20Hz to 20480Hz, starting at 452Hz."},
    {"ids": [3], "text": "Sets the quality factor Q of the filter, 0.707 to 20."},
    {"ids": [4], "text": "Sets the level the detected envelope has to pass before the cutoff starts to move, -20dB to 0dB, starting at -6dB."},
    {"ids": [5], "text": "Multiplies how far the envelope has passed the threshold, 0.5 to 5, so the cutoff travels further and faster for the same input."}
  ],
  "inputs": [
    {"ids": [0], "text": "The audio to be filtered and followed; sixteen channels are treated separately."}
  ],
  "outputs": [
    {"ids": [0], "text": "The filtered audio."}
  ],
  "expects": {
    "inputs": {
      "0": "continuous"
    },
    "outputs": {
      "0": "continuous"
    }
  }
}
