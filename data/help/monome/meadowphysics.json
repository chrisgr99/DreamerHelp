{
  "plugin": "monome",
  "module": "meadowphysics",
  "description": "Eight interrelated gate sequences driven by one clock, their counts and rules set from a grid controller",
  "params": [
    {"ids": [0], "text": "Sets the internal clock, from a 1 second period at fully counter-clockwise to a 23ms period at fully clockwise; with a cable in the clock input it divides or multiplies that instead, from 1/16 to 16x"},
    {"ids": [1], "text": "Short-press to open the preset view on the grid; press and hold to save the current state into the active preset slot"},
    {"ids": [2], "text": "Click to reclaim a grid that has been taken by another module; the right-click menu is where a grid is first chosen"}
  ],
  "inputs": [
    {"ids": [0], "text": "A clock for the counters; each rising edge above 2.21V pulses them, and the clock knob then divides or multiplies it"}
  ],
  "outputs": [
    {"ids": [0], "text": "The internal clock when it is running, or the external clock after the knob's division or multiplication when one is patched"},
    {"ids": [1, 2, 3, 4, 5, 6, 7, 8], "text": "One of eight gate outputs, 0V low and 8V high, following the counter on its own row of the grid; the internal clock gives it a half-length gate"}
  ],
  "notes": [
    "the eight gate sequences run from the clock alone, but their counts and rules can only be set from a grid. Add a grid module or attach hardware, then pick it from the Device Connection list.",
    "a hardware grid needs serialosc installed; a virtual grid module needs nothing.",
    "a very fast external clock, near a 1ms period, can have pulses skipped; the module will not run at audio rate."
  ],
  "menu": [
    "Firmware Tools — opens a submenu naming the firmware and its version, with the input and output rate settings, a screenshot command, and the firmware reload commands",
    "Input rate — sets how often the module reads its jacks, from 1x at the audio sample rate down to /16; found on the Firmware Tools submenu",
    "Output rate — sets how often the module writes its jacks, from 1x at the audio sample rate down to /16; found on the Firmware Tools submenu",
    "Save PNG screenshot — writes a picture of the module to disk; found on the Firmware Tools submenu",
    "Switch Firmware — loads a different firmware build into the module; found on the Firmware Tools submenu",
    "Hot Reload — reloads the firmware and keeps both the working memory and the stored presets; found on the Firmware Tools submenu",
    "Reload & Restart — reloads the firmware, discards the working memory and keeps the stored presets; found on the Firmware Tools submenu",
    "Clear NVRAM — wipes the module's stored presets and keeps the working memory; found on the Firmware Tools submenu",
    "Device Connection — lists the hardware and virtual grids the module can attach to; pick one to connect it, and the USB jack on the panel then shows a plug in it"
  ],
  "expects": {
    "inputs": {
      "0": "high above 2.21V · stepped · one channel only\n\na negative voltage does nothing here."
    },
    "outputs": {
      "0": "0 to 8V · stepped · one channel only",
      "1": "0 to 8V · stepped · one channel only",
      "2": "0 to 8V · stepped · one channel only",
      "3": "0 to 8V · stepped · one channel only",
      "4": "0 to 8V · stepped · one channel only",
      "5": "0 to 8V · stepped · one channel only",
      "6": "0 to 8V · stepped · one channel only",
      "7": "0 to 8V · stepped · one channel only",
      "8": "0 to 8V · stepped · one channel only"
    }
  }
}
